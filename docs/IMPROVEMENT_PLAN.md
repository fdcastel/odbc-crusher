# ODBC Crusher — Improvement Plan

**Created**: 2026-09-07
**Supersedes**: [old-IMPROVEMENT_PLAN.md](./old-IMPROVEMENT_PLAN.md) (the 2026-04-24 → 2026-04-27 plan, §1–§8; a verbatim copy remains in the gitignored `tmp/old-IMPROVEMENT_PLAN_INITIAL.md`)
**Baseline commit**: `17d6182`
**Scope**: full-codebase review — application (`src/`), mock driver (`mock-driver/`), test architecture (`tests/`), build system, CI/CD, CLI/output contract, documentation.

---

## How to keep this plan updated

This document is the single work queue for the project. **Update it in the same commit as the change it describes** — never in a follow-up commit, or it drifts.

**Every time you finish a task:**

1. Set **Status** to `✅ DONE` (only once the fix is implemented *and* tested — see below).
2. Fill the **Commit** column with the short hash (`git rev-parse --short HEAD`).
3. Rewrite the **Details** cell to say what was *actually* done, not what was planned. If the implementation diverged, say so and why.
4. If the work uncovered new problems, add new rows rather than widening an existing task.

**Status values** — use exactly these:

| Symbol | Meaning |
|---|---|
| ✅ DONE | Fix implemented **and** tested. Not DONE until a test would catch its regression, or Details explains why no test is possible. |
| 🔧 IN PROGRESS | Partially implemented or underway. Note in Details what is left. |
| ❌ OPEN | Not yet addressed. |
| ⏯️ DEFERRED | On hold until another task finishes. Name the blocking task ID in Details. |

**Rules that keep the plan honest** — the previous plan broke each of these at least once:

- **Never mark a task DONE on the strength of a plan entry. Re-read the code.** Prior §2.1 claimed "migrated all 23 test files" while `param_binding_tests.cpp` was 3-of-26 migrated. Prior §3.2/§3.6 claimed "every public entry point takes `HandleLock`" while `SQLFreeStmt` takes none.
- **Marking a task DONE and then re-loosening it elsewhere is not DONE.** Prior §7.10 stayed ✅ while §8 immediately relaxed the very assertion it had tightened.
- **"Implicitly addressed by another task" is not a resolution.** Prior §2.8 was closed that way and is untouched in the code.
- **Never delete a row.** If a task turns out to be wrong, keep it and change Details to explain the reversal.
- **Keep `README.md` counts in sync** whenever probe counts, pass rates, or the driver matrix change (H1–H3).
- **This file now lives in the tracked `docs/` directory** (**H8**, done). `tmp/` remains gitignored and is for scratch artifacts, clones and triage output only — never for living documents.

**Suggested order**: A (result integrity) → D (mock driver) → B (taxonomy) → E (build) → C (duplication) → F/G/H.

### If you only do ten things

Ordered by value per hour, not by section. Each is independently shippable.

| # | Task | Why first |
|---|---|---|
| 1 | **D21** — add `tests/test_connection.cpp` to the mock build | 28 tests, 443 lines, never compiled in the project's history. One line of CMake. |
| 2 | **E1** + **D16** — apply the existing warning interface to all 8 targets | The flags already exist and reach 3 targets. Turning them on surfaces 9 real warnings, including an `int → SQLUSMALLINT` narrowing in a `SQLGetInfo` return path. `-Wshadow` alone catches **D14**/**D22**. |
| 3 | **A2** + **C2** — one dialect-fallback helper in `TestBase` | 58 bare `SELECT`s currently make the entire Escape Sequence category, all of Numeric Struct and all of Cursor Stress unusable against Firebird — the driver this repo sits inside. |
| 4 | **G1** — send progress output to stderr | The `-o json \| jq` usage the README documents does not work today. Two lines. |
| 5 | **A9** — catch `std::exception` in `run_test` | One uncaught exception currently discards all 195 results and writes no JSON, and the crash guard blames the driver for it. |
| 6 | **D23** + **D24** — bound the `SQLGetInfoW` memcpy and post `01004` on truncation | A real buffer overflow on the only `SQLGetInfo` path Windows exposes, plus the most likely cause of the §8.2 Unicode probe failures. |
| 7 | **A1** — stop turning a wrong value into `SKIP_INCONCLUSIVE` | The largest false-negative class in the suite; SKIP does not affect the exit code. |
| 8 | **D3** — bring `register.sh` to parity with `register.ps1` | One template edit; the single input that differs between the passing Windows CI slot and the failing Linux one. |
| 9 | **B1** + **B2** — fix or reclassify the ≥26 always-PASS probes | They put a fixed ~13% floor under every driver's advertised score. |
| 10 | **H1**–**H5**, **H8** — documentation truth pass | The README claims 191/195 (it is 195/195) and that Firebird is not in the matrix (it is); there is no LICENSE file; and this plan lives in a gitignored directory the project's own PROJECT_PLAN points at. |

---

## How these findings were verified

Everything below was checked against the running system, not read off the previous plan.

| Check | Result |
|---|---|
| Clean rebuild of both projects, MSVC Debug | 0 errors, 0 warnings **as currently configured** |
| `ctest -L unit` | 27/27 pass (7 skip — no real driver configured) |
| `ctest -L e2e` | 14/14 pass |
| mock-driver `ctest` | 107/107 pass |
| `odbc-crusher` vs `Mode=Success` mock (Windows) | **195/195 PASS**, 0 failed, 0 skipped, 141 ms |
| Rebuild of **every** target with `/W4 /EHsc` | **3** new warnings in the app, **6** in the mock (E1) |
| Latest master CI run `25025987882` | all 14 jobs green |
| `-o json` piped into a JSON parser | **fails to parse** (G1) |
| Bare `SELECT <expr>` statements with no `FROM` fallback | **58**, across 4 probe files (A2) |
| ANSI entry points exported on Linux but not Windows | **38** (D2) |
| Mock test files on disk vs in the build | 10 vs **9** — `test_connection.cpp` (28 tests) never added (D21) |
| Probes that PASS on every branch | **≥26** of 195 (B1) |

The Windows run reproduces 195/195 against the mock. The Linux gap (6 FAIL / 23 SKIP, hard-coded as a tolerance in the e2e canary) was **not** reproducible here — no Linux toolchain on this host — so Linux-specific items rest on code reading plus the CI baseline constants.

---

## Part 0 — Corrections to the previous plan

Its Executive Summary claims that "every task in §1–§5 is now ✅ RESOLVED, ⏯️ DEFERRED with explicit gating, or 🚫 WONTFIX" and that its three headline weaknesses "are all closed". Both are too strong.

| Prior item | Prior status | Verdict | Evidence |
|---|---|---|---|
| §2.1 Extract `run_test`; "migrated all 23 `src/tests/*.cpp` files" | ✅ RESOLVED | **WRONG** | `param_binding_tests.cpp` defines 26 `TestResult` functions; **3** call `run_test`. It retains **14** `make_result` sites and **28** hand-rolled `high_resolution_clock` sites. Repo-wide: 174 `run_test` vs 15 `make_result` + 36 manual-timing. The plan's own §6.5 says "14 of 25 unmigrated", contradicting §2.1's headline. The other 22 files *are* fully migrated. → **C1** |
| §2.8 Collect failing query text in retry loops | ✅ RESOLVED ("implicitly addressed by §2.1") | **WRONG** | `run_test` centralises the *outer* catch. The ~33 dialect-fallback loops each carry their own **inner** `catch (const OdbcError&) { continue; }` discarding query, SQLSTATE and message (`datatype_tests.cpp:57-59`). Nothing in §2.1 touches them. → **C2** |
| §3.2 / §3.6 "Every public entry point in `statement_api.cpp` takes `HandleLock` and clears diagnostics on entry; `SQLFreeStmt` skips the clear on `SQL_DROP`" | ✅ RESOLVED | **WRONG on the first half** | `SQLFreeStmt` (`mock-driver/src/odbc/statement_api.cpp:1644-1677`) takes **no `HandleLock` for any option** — the `fOption != SQL_DROP` guard wraps only `clear_diagnostics()`. `SQL_CLOSE`/`SQL_UNBIND`/`SQL_RESET_PARAMS` mutate containers that a concurrent `SQLFetch` is iterating. 17 of the other 18 entry points do comply. → **D4** |
| §7.10 Promote the `Mode=Success` canary to `failed == 0 && skipped == 0` | ✅ RESOLVED | **SUPERSEDED — not true on Linux** | §8 re-loosened it to `kMaxFailed=6 / kMaxSkipped=23` (`tests/e2e/test_e2e_scenarios.cpp:79-80`). The contract holds on Windows/macOS only, and the "improvement detector" only writes to stderr, so the ratchet can never tighten itself. → **D1**, **B5** |
| §4.1 "Add static analysis (clang-tidy or CodeQL)" | ✅ RESOLVED | **PARTIALLY** | CodeQL runs and is correctly scoped. But the project's own `/W4 /WX` + `-Wall -Wextra -Wconversion` interface reaches only **3 of 8** targets: `src/reporting/`, `src/main.cpp`, `tests/` and **all of `mock-driver/`** compile with default warnings. Rebuilding with `/W4` surfaces 9 real warnings. → **E1** |
| §1.4 `verify_rows_persisted` closes the silent-corruption blind spot | ✅ RESOLVED | **PARTIALLY** | The helper exists and the canaries prove it trips. But it reads into `char buf[256]` and accepts `SQL_SUCCESS_WITH_INFO`, so a value ≥256 chars is silently truncated into a CRITICAL false mismatch; NULL rows are stored as `std::string()`, indistinguishable from `""`, though the header advertises it for NULL-vs-empty work (`test_base.cpp:82-93`). Used by one file only. → **A8** |
| "Weakness #1: silent-corruption blind spot → closed" | summary claim | **OVERSTATED** | The *round-trip* blind spot is closed. The larger one is untouched: a probe that executes fine and reads back a **wrong value** reports `SKIP_INCONCLUSIVE`, which does not affect the exit code. → **A1** |
| §1.1 "Full int→string / float→string round-trip **matrix**" | ✅ RESOLVED | **OVERSTATED** | The two templates exist and are well built, but 9 of ~18 cells are present. CHAR and WVARCHAR are exercised with `SQL_C_SLONG` only (1 of 4 integer C types); floats only against VARCHAR; no unsigned C types. The WVARCHAR cell reads back through `SQL_C_CHAR`, so the W path is half-covered. → **A20** |
| §1.7 `test_sqldescribeparam_{varchar,integer,decimal}` | ✅ RESOLVED | **PARTIALLY** | All three exist — and **none asserts anything**. Each prints "(expected SQL_VARCHAR=… or SQL_WVARCHAR=…)" beside the actual value and never compares them (`param_binding_tests.cpp:850-857, 934-941, 1154-1161`). The claim delivered probe names, not probes. → **B1** |
| §1.6 `test_varchar_raw_byte_integrity` "informational (always PASS)" | ✅ RESOLVED | **CORRECT for that cell; pattern unmanaged** | Its rationale justifies dropping *one* assertion, not all of them — `ind == 8` and `memcmp(raw,"ABCDEFGH",8)` hold on every engine. Meanwhile **at least 26 other probes** always PASS without saying so, and all count toward the advertised pass rate. → **B1**, **B2** |
| PORT §4.6 "driver-detected per-row failure" probe | ✅ RESOLVED | **WRONG (first half)** | `test_param_status_per_row_partial_failure` cannot detect a driver-detected failure: `ODBC_TEST_ARRAY` is created with no PK, no UNIQUE and no NOT NULL (`array_param_tests.cpp:47`), so no row can violate anything and the probe green-paths to PASS on every real driver. Its sibling `test_paramset_size_unsupported_returns_error` **is** genuinely good. → **B1** |
| PORT §4.12 "**consolidated** scalar-function claim-vs-execute matrix" | ✅ RESOLVED | **PARTIALLY** | The matrix probe exists and works, but it was *added alongside* the four per-category probes rather than consolidating them — the bitmask walk and query table are now duplicated five ways. It also has an empty `if` body (`escape_sequence_tests.cpp:1145-1147`) and a `total_passed` that is computed and never read. → **C12** |
| §8.1 Linux diagnostics: "likely cause — unixODBC pulls via legacy `SQLError`, which the mock does not export" | ❌ OPEN (hypothesis) | **HYPOTHESIS IS WRONG — do not implement `SQLError` first** | `SQLError` genuinely does not exist (0 hits in `mock-driver/src/`), but it cannot be the cause: unixODBC falls back to the 2.x path only when *neither* `SQLGetDiagRec` nor `SQLGetDiagRecW` resolves, and the Linux `.so` exports **both**. Three code-verified causes rank ahead of it. (1) **The diagnostic content is broken, not the routing**: `SQL_DIAG_ROW_COUNT` is hard-zero because `StatementHandle::row_count_` shadows `OdbcHandle::row_count_` (`handles.hpp:37` vs `:113`; every writer uses the derived member, `diagnostic_api.cpp:127` reads the base), and `return_code_`, `dynamic_function_`, `dynamic_function_code_`, `cursor_row_count_` are **never written anywhere**. That alone explains three of the eight probes. (2) **Registration parity**: `register.ps1` writes `APILevel`, `ConnectFunctions`, `DriverODBCVer`, `SQLLevel`; `register.sh` writes none — the one input that differs between the passing Windows slot and the failing Linux slot. (3) **Diagnostics are cleared on entry by every mock entry point**, so any DM-internal call between the failing call and the app's `SQLGetDiagRec` wipes the stack — which is exactly why the mock's own gtests pass in isolation while DM-routed calls fail. **Next step**: `ltrace` the `.so` to see which of `SQLGetDiagRec`/`SQLGetDiagRecW` unixODBC actually calls and when; that single observation discriminates between (2) and (3). Separately, the 38-function export asymmetry is a real HIGH defect in its own right. → **D2**, **D3**, **D21**, **D22** |
| §7.1 Verify the postgresql + mariadb `apt_version` pins | ❌ OPEN | **STILL OPEN; description now stale** | The pins have since changed (`1:16.00.0000-0.4-1build1` → `1:16.00.0000-1`; `3.1.15-1build3` → `3.1.15-3`); `.github/drivers.json` last changed 2026-04-27, while the last *successful* postgresql/mariadb/mysql/clickhouse stress-test jobs are from **2026-02-09**. The new pins have never run. → **F1** |
| §7.3 Decide the fate of `fetch-stress-test.ps1` | ❌ OPEN | **STILL OPEN; drift is now real** | Its `$Drivers` hashtable has no `firebird` entry while the manifest does. It also writes to a `recommendations/` directory that no longer exists. → **F5** |
| §5.1 Retire the 23 wrappers; add the e2e harness | ✅ RESOLVED | **CONFIRMED** | Wrappers gone, `tests/unit` + `tests/e2e` split real, 14 scenarios pass locally. No action. |
| §2.11 Remove the `TestStatus::SKIP` alias | ✅ RESOLVED | **CONFIRMED** | No remnant; all three `switch`es enumerate every value. Footnote: the `is_skipped()` helper added for it has **zero call sites**. → **C6** |
| §3.3 `read_param_value` pointer guards | ✅ RESOLVED | **CONFIRMED** | Element-size, stride and `strnlen`-with-1 MB-cap guards all present and correct (`statement_api.cpp:76, 82, 178-189, 202-208`). |
| §3.12 "CI checks every `.def` export has an implementation" | ✅ RESOLVED | **CONFIRMED but one-directional — and it hid a bigger problem** | The check verifies `.def → source` only. The reverse is unchecked, and **38 `SQL_API` functions are defined in the sources but absent from the `.def`**. Its extraction regex also silently skips any export not indented with exactly four spaces. → **D2**, **F11** |
| §3.13 "`FailOn` honoured by the 8 catalog functions" | ✅ RESOLVED | **CONFIRMED, but inert by default** | All eight call sites exist (`catalog_api.cpp:215, 274, 364, 437, 499, 567, 642, 681`). However `DriverConfig::should_fail` returns `false` unconditionally for `BehaviorMode::Success` (`config.cpp:30-31`) — the default — so `FailOn` does nothing unless `Mode=Partial` is also set. The main `README.md:205` says so; `mock-driver/README.md:26` does not even list `Partial` as a valid `Mode`. → **D26** |
| §3.7 "Connection-string parser hardened; 11 adversarial tests" | ✅ RESOLVED | **PARTIALLY** | The 11 tests exist and pass, but `in_braces` is a `bool` toggled by a `{` *anywhere*, not only at value start (`config.cpp:67,70-75`): `Key=a{b;Mode=Failure` swallows the rest of the string and `Mode` is never parsed. The ODBC `}}` escape is unhandled. And `Latency=10s` silently means 10 **ms**, `Latency=500us` means 0 ms (`config.cpp:189-191`). None of these is covered by the 11 tests. → **D27** |
| §7.8 UPDATE/DELETE real row counting; §7.9 multi-tuple INSERT | ✅ RESOLVED | **CONFIRMED** | `make_where_predicate` and `split_value_tuples` are both correct as described. Caveats tracked separately (**D11**, **D12**). |
| §3.5 `~StatementHandle` frees only `SQL_DESC_ALLOC_AUTO` descriptors | ✅ RESOLVED | **CONFIRMED but dead code, guarding the wrong direction** | `SQL_DESC_ALLOC_USER` is never assigned anywhere, and `SQLSetStmtAttr` cannot accept `SQL_ATTR_APP_ROW_DESC`, so the guarded scenario is unreachable. The *reachable* double-free — an app freeing an implicit descriptor via `SQLFreeHandle` — is unguarded. → **D8** |
| §3.10 WONTFIX data-driven mock schema; §4.6 WONTFIX install headers | 🚫 WONTFIX | **CONFIRMED reasonable** | No action. |

---

## Part A — Probe result integrity

The tool's entire value is that its verdicts are trustworthy. These are cases where a verdict is wrong. **Highest priority.**

| ID | Status | Sev | Area | Task | Details / Evidence | Commit |
|---|---|---|---|---|---|---|
| A1 | ❌ OPEN | HIGH | Classification | **Stop classifying a wrong value as SKIP** in dialect-fallback loops | The shape is `for (q : queries) { try { execute; if (ok && value == expected) { success = true; break; } } catch { continue; } }` — "executed fine, returned 41 instead of 42" falls through exactly like "did not execute", and the probe ends as `SKIP_INCONCLUSIVE: "No compatible query pattern found"`, which does **not** affect the exit code. Largest false-negative class in the suite. `datatype_tests.cpp:44-59` and all 9 DataType probes, plus `statement_tests.cpp:161`. **Fix**: once `execute()` returns, the probe is conclusive — compare and FAIL; advance to the next dialect only when `execute()` threw. | |
| A2 | ❌ OPEN | HIGH | Portability | **58 bare `SELECT <expr>` statements have no Firebird fallback** — four probe categories are unusable against the driver family this repo serves | 15 of 23 probe files carry the `FROM RDB$DATABASE` variant. The 4 that execute bare selects and do not: **`escape_sequence_tests.cpp` (46 sites — the entire 20-probe Escape Sequence category)**, `numeric_struct_tests.cpp` (6), `cursor_stress_tests.cpp` (5), `transaction_tests.cpp` (1, at `:588` inside the CRITICAL rollback-with-open-cursor probe). Consequences differ by file: numeric_struct throws → 4 probes report **ERROR**; cursor_stress swallows the throw → `successful == 0` → **FAIL** "cursor exhaustion issues" (`:85`) and "cursor state corruption" (`:283`); escape probes report `'NULL'`. Fix once via the shared helper in **C2**. | |
| A3 | ❌ OPEN | HIGH | Buffer safety | Driver-supplied lengths used as buffer lengths — stack over-read on truncation | `escape_sequence_tests.cpp:81` `std::string(reinterpret_cast<char*>(buf), ind)` where `ind` is the **total available** bytes, not bytes written; `SQL_SUCCEEDED` accepts the `01004` truncation warning, and `SQL_NO_TOTAL (-4)` converts to `SIZE_MAX-3`. Same shape at `:66` for `SQLNativeSql`'s `StringLength2Ptr`, and at `connection_tests.cpp:32,56` for `SQLGetInfo`. Firebird's `SQL_DATABASE_NAME` is a filesystem path, so >255 is realistic. In a tool whose stated philosophy is "never crash" — and where `main.cpp:68-83` would report the resulting fault as a **driver** crash. **Fix**: clamp to `sizeof(buf)-1`, handle `SQL_NO_TOTAL`, and treat truncation as its own outcome. | |
| A4 | ❌ OPEN | HIGH | Diagnostics | The escape-sequence helpers throw away every SQLSTATE | `escape_sequence_tests.cpp:84-86` (`catch (...) { return std::nullopt; }`) and `:68`. A failed scalar function is reported only as `UCASE='NULL'`, with no SQLSTATE and no failing SQL. This is why the DuckDB triage had to be resolved by reading driver source to find `SetNotImplemented`/`HYC00`. **Fix**: return `{value, sqlstate, diag}` and let callers map `HYC00`/`IM001` → SKIP_UNSUPPORTED, `42000` → FAIL. Highest-leverage single fix in that file. | |
| A5 | ❌ OPEN | HIGH | Probe logic | `test_null_termination` cannot detect what it tests, and over-reads the stack when it should fire | `buffer_validation_tests.cpp:41,61,63`: buffer is `memset` to `'X'` (no NUL), then `std::strlen(buffer)` — UB precisely when the driver fails to terminate. The follow-up `buffer[strlen(buffer)] != '\0'` is a tautology, so the FAIL branch is unreachable. **Fix**: `memchr(buffer, 0, sizeof buffer)`; FAIL if null; compare `buffer_length` to that offset. | |
| A6 | ❌ OPEN | HIGH | Probe logic | `test_bind_col_integer` PASSes any non-NULL value | `statement_tests.cpp:387`: `if (value == 42 \|\| indicator != SQL_NULL_DATA)`. The right operand is true for every non-NULL fetch. Should be `&&`. | |
| A7 | ❌ OPEN | HIGH | Probe logic | `test_fetch_bound_vs_getdata` never compares the two values | `statement_tests.cpp:468,477-485`: `expected` says "values match"; **both** branches set PASS and no comparison happens; `SQLBindCol`'s rc is discarded. **Fix**: check the rc; FAIL on mismatch; SKIP only on 07009/HY109 with `SQL_GETDATA_EXTENSIONS` lacking `SQL_GD_BOUND`. | |
| A8 | ❌ OPEN | HIGH | Probe logic | `test_paramset_size_one` FAILs at Core for two optional Level-1 attributes it never checked | `array_param_tests.cpp:875-876` ignores both `SQLSetStmtAttr` return codes; `:896-901` then FAILs when `processed != 1` or `status != SQL_PARAM_SUCCESS`. If the driver declines the optional attributes, `processed` stays 0 and `status` stays `0xFFFF` → guaranteed FAIL for a correct driver. The sibling probe 90 lines up (`:634-640`) checks the same rc and SKIPs — the two disagree. | |
| A9 | ❌ OPEN | HIGH | Robustness | `run_test` catches only `OdbcError`; any other exception destroys the whole run **and the report** | `test_base.hpp:124-137`. `crash_guard.cpp:18-26` deliberately lets C++ exceptions through and `main.cpp:240` turns them into `return 3` — no summary, and with `-o json -f` **no file at all** (the report is serialised only in `report_end`). One `std::bad_alloc` in probe #4 discards the other 191 results, and the crash guard misattributes it to the driver. Contradicts `AGENTS.md`'s "never crash". **Fix**: add `catch (const std::exception&) → ERR`; this also deletes ~45 hand-written inner catches (**C3**). | |
| A10 | ❌ OPEN | HIGH | Portability | `test_wchar_surrogate_pair_preserved` is wrong on 4-byte `SQLWCHAR` builds | `unicode_tests.cpp:441-456` (`make_wchar_buf`) always emits a UTF-16 surrogate *pair*; used at `:650`/`:689` with `U+1F600` and asserted at `:676`/`:715`. On a unixODBC build where `SQLWCHAR == wchar_t == 4 bytes`, the payload is two **lone surrogates** — ill-formed UTF-32 that iconv rejects or replaces, so a correct driver FAILs. Its sibling `test_wchar_roundtrip_non_ascii` is width-correct (BMP only) — verified. **Fix**: `#if` on `sizeof(SQLWCHAR)`; emit the scalar and expect one unit, or SKIP_UNSUPPORTED there. | |
| A11 | ❌ OPEN | HIGH | Probe logic | `cursor_behavior_tests.cpp:56` FAILs a spec-compliant driver | `while (SQLFetch(...) == SQL_SUCCESS)` exits mid-result-set on any row carrying `SQL_SUCCESS_WITH_INFO` (`01004`, `01S07`, driver `01000`); the confirming fetch at `:63` then returns success again and `:69` FAILs. The 10,000-row safety break at `:58` produces the same false FAIL on any real table larger than that. Same `== SQL_SUCCESS` pattern without the FAIL consequence at `catalog_depth_tests.cpp:53,128,168,204,255`, `unicode_tests.cpp:271,331`, `metadata_tests.cpp:99,358,461,842,902`, `statement_tests.cpp:342`, `escape_sequence_tests.cpp:883`. **Fix**: `SQL_SUCCEEDED` everywhere; distinguish "hit the cap" from "reached the end". | |
| A12 | ❌ OPEN | HIGH | Test design | The cursor-stress thresholds silently disable themselves on fast drivers and flake on loaded runners | `cursor_stress_tests.cpp:206-207` computes `open_mean = open_total_us / successful` in integer microseconds. On an in-process driver (mock, DuckDB) `open_mean` truncates to `0`, and the `open_mean > 0` guard at `:218` then makes the only FAIL condition **unreachable** — the probe degrades to an unconditional PASS exactly where a close-path regression would show. When it is reachable, the bare 10× ratio with no absolute floor trips on two scheduler preemptions across 500 iterations. Separately, `test_handle_reuse_no_leak` scans the diagnostic queue *after* `SQLCloseCursor` (`:262`), so it measures records posted by that one call — it cannot detect accumulation by construction — and `> 1` (`:291`) FAILs a driver that legitimately posts two warnings. **Fix**: accumulate nanoseconds and compare totals (no division); add an absolute floor; use a trimmed statistic; sample the queue *before* the next `SQLExecDirect`. | |
| A13 | ❌ OPEN | MED-HIGH | Spec accuracy | The NUMERIC byte-equality probe rounds where the spec truncates | `numeric_struct_tests.cpp:417-419` uses `std::llround(literal * 10^got.scale)`. When a driver legally reports a smaller `got.scale` (returning `SQL_SUCCESS_WITH_INFO` + `01S07` fractional truncation), the ODBC conversion rule is **truncate**: for `12345.67` at `scale=0` a compliant driver returns `12345` and the probe expects `12346`. Also, the comment at `:341-347` claiming mantissas "fit in 32 bits" is false — variant 3 yields 1,234,567,890,000 (41 bits); harmless today but misleading for the next person adding a variant. | |
| A14 | ❌ OPEN | MED | Transaction hygiene | `AutocommitForDdl` turns autocommit **off** when it cannot read the current setting | `test_base.cpp:134,147`: `SQLUINTEGER saved_ = 0;` and the `SQLGetConnectAttr` rc is ignored; `SQL_AUTOCOMMIT_OFF == 0`. A driver that rejects the read gets autocommit disabled by the destructor, so every later category runs inside an open transaction — the state Firebird's DDL handling cannot survive. Same at `transaction_tests.cpp:22-23`. Related: `array_param_tests.cpp:106-107` forces autocommit ON in the drop path and never restores it; `param_binding_tests.cpp:1034-1041` restores to a hard-coded ON rather than the saved value. | |
| A15 | ❌ OPEN | MED | Probe logic | Stale tables from a crashed run cause false FAILs | `transaction_tests.cpp:29-40` reuses `ODBC_TEST_TXN` without clearing it, while `:273` asserts `count == 1` and `:379` asserts `count == 0`. `array_param_tests.cpp:29-40` does the same with `ODBC_TEST_ARRAY`, and its probe rows 9990-9992 are never deleted, so `:418`'s `WHERE ID IN (9991,9992)` can return 4 rows and FAIL a correct driver at `:428`. The crash guard keeps the process alive after an abort, so cleanup is skipped and the *next* run misreports. **Fix**: `DELETE FROM` after the reuse path (and adopt **C7**). | |
| A16 | ❌ OPEN | MED | Spec accuracy | OUT/INOUT parameters are read without draining `SQLMoreResults` | `escape_sequence_tests.cpp:961-967`, asserted at `:1034` and `:1212`. A driver may legally defer populating output-parameter buffers until every result set from the procedure is consumed. A conformant driver whose SP body returns a result set is FAILed at `Severity::ERR` for "did not write to the bound buffer". **Fix**: `while (SQLMoreResults(h) == SQL_SUCCESS) {}` before inspecting. | |
| A17 | ❌ OPEN | MED | Coverage | `{?=CALL …}` is never executed — only string-translated | All three §4.3 direction probes call `{CALL MOCK_INOUT(?, ?, ?)}` (`escape_sequence_tests.cpp:917`); the `{?=CALL}` form appears only as input to `SQLNativeSql` (`:308, 794, 833`). The classic driver defect — binding the first argument as parameter 1 instead of 2, because parameter 1 is the return value — cannot be detected. Blocked in part by prior PORT 3.E (mock has no callable *function*). | |
| A18 | ❌ OPEN | MED | Probe logic | `test_sqlprocedurecolumns_smoke` FAILs spec-valid input | `metadata_tests.cpp:907-914,938-943`: the `COLUMN_TYPE` switch omits `SQL_PARAM_TYPE_UNKNOWN` (0), which the spec lists, so it lands in `other_count` → FAIL. The `fetch_int_col` helper at `:785-791` returns `0` for a NULL value *and* for a failed `SQLGetData`, so unreadable rows count as bogus type codes too. **Fix**: add the case; return `std::optional`. | |
| A19 | ❌ OPEN | MED | Helper correctness | Harden `verify_rows_persisted` | `test_base.cpp:82-93`: values read into `char buf[256]`; `SQL_SUCCEEDED` accepts `01004`, so a ≥256-char value is silently truncated and reported as CRITICAL data corruption. NULL rows become `std::string()`, indistinguishable from `""`, despite `test_base.hpp:161-162` advertising it for NULL-vs-empty work. Identifiers interpolated unquoted. **Fix**: loop on `01004`, return `vector<optional<string>>`, quote with `SQL_IDENTIFIER_QUOTE_CHAR`. | |
| A20 | ❌ OPEN | MED | Coverage | Finish the round-trip matrix and verify the array string axis | Prior §1.1 shipped 9 of ~18 cells: CHAR and WVARCHAR only with `SQL_C_SLONG`, floats only against VARCHAR, no unsigned C types, and the WVARCHAR cell reads back through `SQL_C_CHAR` so the W path is half-covered. Separately, `array_param_tests.cpp:487-501` executes the PARAMSET_SIZE=3 string run and inspects only `exec_ret` — the integer safety probe *is* verified (`:417-444`), but the string axis the file's own header warns about is not. `verify_rows_persisted` is called **zero** times in `array_param_tests.cpp` and `escape_sequence_tests.cpp`. | |
| A21 | ❌ OPEN | MED | Probe logic | Several probes accept the wrong answer | `datatype_edge_tests.cpp:484-499` (`test_string_as_integer` — both comparison branches PASS, so `0` for `'123'` passes); `:212` (`test_varchar_empty` accepts `SQL_NULL_DATA` as "empty string", the exact conflation its sibling probe FAILs); `:267-273` (`test_varchar_special_chars` retrieves the value and never compares it — and its literal `'a''b"c\d'` is dialect-dependent on MySQL); `boundary_tests.cpp:244-256` (`SQLDescribeCol(col 0)` PASSes both `SQL_ERROR` without reading the state and plain `SQL_SUCCESS`, where the spec requires `07009`); `unicode_tests.cpp:347-431` (claims to check `01004` in both `expected` and `actual` but only tests `ret == SQL_SUCCESS_WITH_INFO`, so any unrelated warning satisfies it). | |
| A22 | ❌ OPEN | MED | Transaction hygiene | `SQLEndTran(SQL_COMMIT)` rc discarded before verification | `param_binding_tests.cpp:471, 646, 1248, 1551, 1678, 2003`. When the commit fails, `verify_rows_persisted` reports 0 rows and the probe blames the *bind path* ("this is the Firebird #161 silent-corruption shape", `:481-483`) for what was a commit failure. `:1819` is the one site that captures it correctly. Also `transaction_tests.cpp:266-280,372-386`: if the count fetch yields no row the status stays at `run_test`'s PASS default with an empty `actual`. | |
| A23 | ❌ OPEN | MED | Probe logic | Probes that measure the Driver Manager, not the driver | `sqlstate_tests.cpp:48-80,82-119,121-178,278-324,404-437,439-477`, `state_machine_tests.cpp:43-92`, `error_queue_tests.cpp:139-228`. Both the Windows DM and unixODBC enforce the Appendix B state table before dispatching, so these PASS for every driver — including one that implements none of it. **Fix**: retag as DM-conformance and exclude from the driver score (needs **B2**), or redesign to reach the driver as `sqlstate_tests.cpp:180-235` already does. | |
| A24 | ❌ OPEN | MED | Portability | Hard-coded mock schema and non-portable DDL | `"CUSTOMERS"` exists only in `mock-driver/src/mock/mock_catalog.cpp:153`, yet is passed as a real table to `SQLStatisticsW`/`SQLTablePrivilegesW`/`SQLColumnPrivilegesW` (`catalog_depth_tests.cpp:114,199,213`) — 0 rows on every real driver, and the probes still PASS. Catalog functions match case-sensitively unless `SQL_ATTR_METADATA_ID` is set, so even a real `customers` would not match. Separately, `RoundTripTableGuard` emits `CREATE TABLE t (ID …, VAL …)` with no `ENGINE=` clause, so **every** guard-based probe SKIPs on ClickHouse; and `DECIMAL(19,0)` / `DECIMAL(38,10)` (`numeric_struct_tests.cpp:350-351`) exceed Firebird 3's precision limit of 18. | |
| A25 | ❌ OPEN | MED | Spec accuracy | `SQL_DIAG_NUMBER` read into an `SQLLEN` | `diagnostic_depth_tests.cpp:92-95`. `SQL_DIAG_NUMBER` is an `SQLINTEGER` header field (4 bytes); `SQLLEN` is 8 on LP64/LLP64, so the driver writes 4 bytes into an 8-byte slot. Works today only because the variable is zero-initialised and the platform is little-endian. The neighbouring `SQLLEN row_count` for `SQL_DIAG_ROW_COUNT` at `:144` **is** correct. | |
| A26 | ❌ OPEN | LOW | Hygiene | Small correctness and portability nits | `metadata_tests.cpp:842-847,902-919` walk `SQLProcedures`/`SQLProcedureColumns` uncapped (thousands of `pg_catalog` rows on PostgreSQL); `advanced_tests.cpp:356-360` treats `SQL_NO_DATA` on `SELECT 1` as PASS; `sqlstate_tests.cpp:103,107` cites 24000 where the spec's transition table gives HY010 for state S1; `numeric_struct_tests.cpp:24-32` and `:537-540` shift all 16 `val[]` bytes into a `uint64_t`, silently dropping bytes 8-15 in a probe named for extremes; `datatype_edge_tests.cpp:781-782` puts `0xDEADBEEF` into a signed `SQLINTEGER`; `escape_sequence_tests.cpp` relies on transitive includes for `<cmath>` (if only integer `abs` is visible, `std::abs(3.14-3.0)` truncates to 0 and every numeric comparison passes unconditionally), `<string>`, `<vector>`, `<optional>`; `unicode_tests.cpp:156`/`datatype_edge_tests.cpp:634` use `std::min` without `<algorithm>`; `statement_tests.cpp:380-384,424-428,466-468` and `param_binding_tests.cpp:1639-1642,1970-1973` leave bound buffers dangling after `break`/scope exit; `std::hex` is set and never reset at `array_param_tests.cpp:582,809,983` and `escape_sequence_tests.cpp:206`. | |

---

## Part B — Result taxonomy and reporting honesty

`README.md` advertises a pass rate. Today it is inflated by probes that cannot fail and deflated by probes that SKIP when they should FAIL.

| ID | Status | Sev | Area | Task | Details / Evidence | Commit |
|---|---|---|---|---|---|---|
| B1 | ❌ OPEN | HIGH | Taxonomy | **At least 26 probes PASS on every branch** — audit and fix | Counted across the reviewed files: 15 of 50 in the boundary/datatype-edge/unicode/catalog/diagnostic/cursor/numeric group, 11 of 54 in param-binding/array/escape, plus 8 in the connection/statement/metadata/transaction/advanced/error-queue/state-machine group. Worst offenders: `boundary_tests.cpp:148` (`SQLBindParameter` with a NULL pointer + `SQL_NULL_DATA` **must** return `SQL_SUCCESS` per spec — the `else` at `:181` is a second PASS instead of a FAIL) and `:191`; `error_queue_tests.cpp:51-61` (PASSes even when `SQLGetDiagRec` returns `SQL_ERROR`, reporting "SQLGetDiagRec functional"; its `SKIP_INCONCLUSIVE` at `:29-30` is dead code); `param_binding_tests.cpp:850-857,934-941,1154-1161` (the three `SQLDescribeParam` probes print expected beside actual and never compare); `escape_sequence_tests.cpp:693,727,755` (`test_like_escape_sequence`, `test_outer_join_escape`, `test_interval_literal_escape` are named for escapes they never emit — they read `SQLGetInfo` bitmasks); `statement_tests.cpp:600-601`; `array_param_tests.cpp:1022`. Each is either a missing FAIL branch (most have an obvious one) or genuinely informational and needs **B2**. | |
| B2 | ❌ OPEN | HIGH | Taxonomy | Add a status that is reported but excluded from the pass rate | Several probes are legitimately informational (`test_varchar_raw_byte_integrity`, `test_count_star_result_metadata`, the DM-conformance probes in **A23**). The only way to express that today is `PASS`, so ~13% of the suite is a fixed floor under every driver's score (`main.cpp:47-49` → `json_reporter.cpp:67`). Adding a fifth `TestStatus` touches `test_base.hpp`, both reporters, `main.cpp`'s tally and the e2e canary — do it once, deliberately. | |
| B3 | ❌ OPEN | HIGH | Classification | Add `TestBase::classify_failure()` and use it everywhere | **Not one probe in the codebase reads a SQLSTATE before choosing SKIP vs FAIL** — ~40 sites. Representative: `catalog_depth_tests.cpp:46-50,117-121,248-252` (`SQLTables`/`SQLStatistics` are **Core**); `diagnostic_depth_tests.cpp:65-68` (a preceding `SQL_ERROR` must carry record 1; `SQL_NO_DATA` here is a hard FAIL); `datatype_edge_tests.cpp:493-499,627-632` (required Core conversions); `numeric_struct_tests.cpp` ×8 (`SQL_C_NUMERIC` is a required Core target type); `statement_tests.cpp:690,105`; `metadata_tests.cpp:60`; `param_binding_tests.cpp` ×12; `array_param_tests.cpp` ×8. **Fix**: one helper mapping `IM001 / HYC00 / HY092 / HY106` → SKIP_UNSUPPORTED and everything else → FAIL, always writing the state into `r.diagnostic`. `array_param_tests.cpp:1176-1195` and `metadata_tests.cpp:808-818` already do this correctly — extract from them. | |
| B4 | ❌ OPEN | MED | Classification | Stop converting infrastructure errors into "driver doesn't support it" | `metadata_tests.cpp:548-552,589-593` wrap whole probes in `catch (OdbcError) → SKIP_UNSUPPORTED`; the in-file comments at `:435-436` and `:557` admit it. A failed `SQLAllocHandle` is reported as "Foreign keys not supported by driver". Same shape at `cursor_stress_tests.cpp:56` (`catch (...) {}` swallows `std::bad_alloc` and then blames the driver via the `successful < 90` FAIL at `:85`). | |
| B5 | ❌ OPEN | MED | Test architecture | Make the Linux e2e tolerance a ratchet that can tighten | `tests/e2e/test_e2e_scenarios.cpp:79-80` hard-codes `kMaxFailed=6 / kMaxSkipped=23`; the improvement detector at `:101-118` only writes a `[notice]` to stderr and never fails, so nobody is forced to lower the bound when a gap closes. **Fix**: fail when the observed count drops **below** baseline (an "unexpectedly good" failure forcing the constant down), or move the baseline to a checked-in file the test diffs. Depends on **D1**. | |
| B6 | ❌ OPEN | MED | Classification | Report partial success instead of discarding it | `numeric_struct_tests.cpp:364-370`: variants run (10,2) → (19,0) → (38,10); a table-creation failure on variant 3 returns `SKIP_INCONCLUSIVE` and **throws away** the two variants that already byte-matched. On Firebird 3 (max precision 18) variants 2 and 3 cannot be created at all, so the probe always reports SKIP there. **Fix**: accumulate per-variant outcomes. | |
| B7 | ❌ OPEN | MED | Classification | Reconcile contradictory verdicts and wrong conformance tags | A non-HY010 response to the same probe shape is PASS at `state_machine_tests.cpp:72-76` and FAIL at `sqlstate_tests.cpp:65-68`, in the same run. `catalog_depth_tests.cpp:107` tags `SQLStatistics` as `LEVEL_1`; it is in the ODBC 3.x **Core** interface-conformance list, so a Core gap is reported as optional in every report. | |
| B8 | ❌ OPEN | LOW | Reporting | Fix misleading `expected` text, names and spec references | `advanced_tests.cpp:29,226,376-378,478`; `transaction_tests.cpp:418`; `error_queue_tests.cpp:73`; `connection_tests.cpp:37` (a "SQLGetInfo succeeded" suggestion inside the failure branch); `param_binding_tests.cpp:249` (`test_param_rebind_execute` performs **no rebind** — `:305-306` mutates the value and re-executes with no intervening `SQLBindParameter`, making it a duplicate of the bind-once probe under a misleading name); `cursor_behavior_tests.cpp:189,241` (cites `SQL_GD_ANY_ORDER`, which governs column *ordering*, for a repeated-`SQLGetData` test); `escape_sequence_tests.hpp:19-21` (documents `{escape …}` and `{INTERVAL …}` categories no probe executes). | |
| B9 | ❌ OPEN | LOW | Naming | Normalise probe names to `test_snake_case` | `buffer_validation_tests.cpp`, `state_machine_tests.cpp` and `error_queue_tests.cpp` use Title Case with spaces ("Null Termination Test"). The other 20 files use `test_snake_case`, and both the e2e harness and the triage skill look probes up by exact name. | |
| B10 | ❌ OPEN | LOW | Classification | `test_error_clearing` recognises only three hard-coded "old" SQLSTATEs | `error_queue_tests.cpp:208`; any other leftover state (37000, 42601, HY090…) PASSes as "likely info from new op". **Fix**: capture the initial SQLSTATE and compare against it. | |

---

## Part C — Probe infrastructure and duplication

`src/tests/` is 15.4k lines across 23 categories; 8 files exceed 500 lines and `param_binding_tests.cpp` is 2,033. Most of the bulk is one of six copy-pasted shapes. Realistic total saving: **~1,800 LoC (≈12%)**, about a third of it pure boilerplate with no behavioural risk.

| ID | Status | Sev | Area | Task | Details / Evidence | Commit |
|---|---|---|---|---|---|---|
| C1 | ❌ OPEN | HIGH | Boilerplate | Finish the §2.1 migration — `param_binding_tests.cpp` is essentially unmigrated | 26 `TestResult` functions (24 probes + 2 templates); **3** use `run_test`; **14** distinct bodies hand-roll `make_result` + a top-level `elapsed` lambda; `result.duration = elapsed(); return result;` appears **59** times. The blocker named by prior §6.5 (per-early-return cleanup) no longer exists — `RoundTripTableGuard` shipped in `38ff8fc`. `array_param_tests.cpp` and `escape_sequence_tests.cpp` are **fully migrated** (10/10 and 20/20), contrary to the plan's framing. Expect **−145 LoC**. | |
| C2 | ❌ OPEN | HIGH | Diagnostics | Hoist the dialect-fallback loop into `TestBase` and make it record what failed | ~33 hand-rolled copies across 12 files (`statement_tests.cpp` ×11, `datatype_tests.cpp` ×9, `descriptor_tests.cpp`, `cancellation_tests.cpp`, `state_machine_tests.cpp`, `advanced_tests.cpp`, `sqlstate_tests.cpp`, `error_queue_tests.cpp`, `metadata_tests.cpp`, `boundary_tests.cpp`, `cursor_behavior_tests.cpp`, `diagnostic_depth_tests.cpp`, `unicode_tests.cpp`). **Fix**: `execute_first_working(stmt, queries) -> optional<string>` returning the winning query and accumulating `(query, sqlstate, message)` for every failure. One helper closes prior §2.8 properly, gives **A1** a single place to fix, and lets the four fallback-less files (**A2**) join in. **−120 LoC** in scope, more repo-wide. | |
| C3 | ❌ OPEN | MED | Boilerplate | Delete ~45 inner `try/catch (std::exception)` blocks once **A9** lands | `buffer_validation_tests.cpp` (5/5 probes), `error_queue_tests.cpp` (6/6), `state_machine_tests.cpp` (6/6), `sqlstate_tests.cpp` (10/10), the 10 legacy probes in `datatype_edge_tests.cpp`, plus 3 in `transaction_tests.cpp` that catch `OdbcError` and set exactly what `run_test` already sets. ~120 LoC of pure boilerplate, existing only because `run_test` does not catch `std::exception`. Blocked on **A9**. | |
| C4 | ❌ OPEN | HIGH | Duplication | Adopt `RoundTripTableGuard` in the two files that reimplement it | `grep -c RoundTripTableGuard` returns **0** for `param_binding_tests.cpp`, `array_param_tests.cpp` and `escape_sequence_tests.cpp`, though the guard's own doc comment (`test_base.hpp:158-163`) names the first two as its intended callers. Both hand-rolled versions are leakier: `param_binding_tests.cpp:57-119` (63 lines) plus **46** explicit `drop_roundtrip_table` calls, and `array_param_tests.cpp:19-114` (96 lines) whose drop path never restores autocommit. Adopting the guard removes **~205 LoC** and closes the leak paths in **A14**/**A15**. `transaction_tests.cpp:19-108` is a third reimplementation. | |
| C5 | ❌ OPEN | MED | Duplication | Extract `first_sqlstate(handle_type, handle)` | Re-implemented six ways: `sqlstate_tests.cpp:22-33` and `:35-46` (two near-identical functions differing only in handle type), `state_machine_tests.cpp:59-67`, `advanced_tests.cpp:403-409`, `metadata_tests.cpp:810-813,873-876`. `OdbcError::from_handle(...).diagnostics()[0].sqlstate` already does it. Prerequisite for **B3**. | |
| C6 | ❌ OPEN | LOW | Dead code | Remove or adopt unused helpers and includes | `TestBase::time_test` (`test_base.hpp:90-96`) — zero callers. `is_skipped()` (`:236-239`) — zero callers; `main.cpp:51-52` and `console_reporter.cpp:21-22` spell out both variants inline. `SqlWcharBuf::byte_len()` (`sqlwchar_utils.hpp:34`) — zero callers, though `param_binding_tests.cpp:167` hand-computes the same value incorrectly (it includes the terminator). `escape_sequence_tests.cpp:460-479` declares a `FuncTest` struct and `match` lambda that are never used; `:1145-1147` is an empty `if` body; `total_passed` at `:1124,1155` is computed and never read. Unused includes: `<cstring>` in `catalog_depth_tests.cpp`, `diagnostic_depth_tests.cpp`, `cursor_behavior_tests.cpp`, `boundary_tests.cpp`; `<climits>` in `datatype_edge_tests.cpp`; `<array>` in `array_param_tests.cpp`. `SQLINTEGER indicator` at `connection_tests.cpp:127` written and never read. | |
| C7 | ❌ OPEN | MED | Duplication | Extract `discover_tables(limit)` and `get_string(stmt, col)` helpers | The SQLTables discovery block is copy-pasted four times (`metadata_tests.cpp:85-114,348-373,453-470,701-720`) with `DiscoveredTable` declared twice; `unicode_tests.cpp:261-306` is a fifth variant. `test_primary_keys:219-223` and `test_statistics:279-282` skip discovery entirely and try only `RDB$DATABASE` / `information_schema.TABLES` / `sys.tables` — none of which exist on PostgreSQL, DuckDB or ClickHouse, so they PASS as "callable (no PKs)" without ever seeing a table that has one. Similarly "`SQLGetData` into a `std::string`" is written four ways (`unicode_tests.cpp:272-293`, `test_base.cpp:82-94`, `datatype_edge_tests.cpp:268,428`), none of which loops on `01004`. **−70 LoC** and fixes **A24**. | |
| C8 | ❌ OPEN | MED | Duplication | Make `datatype_edge_tests.cpp` table-driven | `:36-571` is ten structurally identical probes — execute-with-fallback → fetch → `SQLGetData` with C-type T → compare → PASS/FAIL/SKIP — differing only in (query, C type, buffer type, predicate, name). A `static constexpr` table plus one 40-line driver collapses 536 lines to ~90. **−300 LoC**, the single largest mechanical win in the tree. | |
| C9 | ❌ OPEN | MED | Duplication | Table-drive the repeated probe clusters in `param_binding` / `array_param` | `test_sqlrowcount_after_{insert,update,delete}` (163 lines, differ only in seed/mutation/expected) ≈ −110; `test_sqldescribeparam_{varchar,integer,decimal}` (254 lines) ≈ −150; the prepare-INSERT + bind-ID/VAL prologue appears 7 times ≈ −90; the array-exec prologue (prepare → bind type → paramset size → status ptr → processed ptr → bind arrays → execute) repeats 8 times at ~35 lines ≈ −140. | |
| C10 | ❌ OPEN | MED | Duplication | Make `RoundTripTableGuard` movable so `unicode_tests.cpp` stops copy-pasting | `:532-580` is a verbatim copy of `:583-629`, and `:650-686` of `:689-724` — the author's comment at `:529-531` explains why ("the guard is non-movable, so we restructure: do the work inline instead"). Making it movable, or adding a two-phase `try_create(val_ddl)`, lets the body be written once. **−85 LoC**. | |
| C11 | ❌ OPEN | LOW | Duplication | Consolidate overlapping and near-duplicate probes | Autocommit is read by `connection_tests.cpp:82-94`, `transaction_tests.cpp:116-137` and `state_machine_tests.cpp:202-217`; "allocate multiple statements" by `connection_tests.cpp:62-73` and `state_machine_tests.cpp:226-254`; `"THIS IS NOT VALID SQL !!! @#$%"` appears four times; `set_numeric_descriptor` exists in `numeric_struct_tests.cpp:36-52` and is re-implemented inline (ignoring all return codes) at `datatype_edge_tests.cpp:850-859`; `test_native_sql_call_escape` (`escape:300-335`) ≈ `test_call_escape_translation` (`escape:786-820`); `test_array_partial_error` (`array:911`) is a near-clone of `test_param_operation_array` (`array:741`) and its own comment concedes it does not test partial failure. | |
| C12 | ❌ OPEN | MED | Duplication | Actually consolidate the scalar-function probes (finish PORT §4.12) | `test_{string,numeric,datetime,system}_scalar_functions` (`escape_sequence_tests.cpp:385-648`, 264 lines) and `test_scalar_function_claim_vs_execute` (`:1059-1183`, 125 lines) duplicate the same bitmask walk and query table five ways; the four identical `passed/tested → FAIL → tested==0 → SKIP` epilogues hoist trivially. One matrix with an optional `expected` column replaces all five. **−250 LoC**. Bitmask-to-string is separately written three times (`:104-156, 743-749, 771-776`). | |
| C13 | ❌ OPEN | LOW | Architecture | Give `TestBase` the connection string so probes can open a sibling connection | Two PORT items (9.B disconnect-with-open-transaction, 10.B pooled-connection recovery) are both ⏯️ DEFERRED for the same reason: "probes get a single `OdbcConnection&`". The plumbing is nearly free — `OdbcConnection::get_environment()` already exists (`src/core/odbc_connection.hpp:26`); `TestBase` just needs the string `main.cpp` already holds. Unblocks both. | |
| C14 | ❌ OPEN | LOW | Style | Fix `sqlwchar_utils.hpp`, and settle the include-order rule | `sqlwchar_utils.hpp:22` maps each UTF-8 *byte* to one UTF-16 code unit — mojibake for any non-ASCII input; the comment says "ASCII subset is fine" but nothing enforces it and `unicode_tests.cpp` includes it. Separately, **all 23** probe `.cpp` files put project headers before the stdlib, contradicting `AGENTS.md:7`; since the convention is uniform and `<windows.h>` must precede `<sql.h>`, the honest fix is to change the rule to describe the code rather than churn 23 files. Three headers also use relative `"../core/…"` includes and pre-C++17 nested namespaces. | |

---

## Part D — Mock driver

The mock is the project's reference fixture and its only regression net. It is 107 unit tests over ~7.9k lines, and the **only** target in the repo compiled with no warning flags at all. It reports 195/195 on Windows — which means the suite currently cannot see any of the defects below.

| ID | Status | Sev | Area | Task | Details / Evidence | Commit |
|---|---|---|---|---|---|---|
| D1 | ❌ OPEN | HIGH | Linux parity | Close the Linux mock↔unixODBC gap (6 FAIL + 23 SKIP) — start with **D2**/**D3** | On Windows the mock satisfies the reference contract (verified locally: 195/195). On Linux 6 probes FAIL and 23 SKIP, absorbed today by a hard-coded tolerance. Prior §8 grouped them into five clusters (diagnostic forwarding, SQLWCHAR width, catalog patterns, cursor behaviour, array params — the array cluster alone is 8 SKIPs and likely one shared precondition). Test the two whole-surface asymmetries first: either could collapse several clusters at once. | |
| D2 | ❌ OPEN | HIGH | Export surface | Give the Linux/macOS `.so` the same export surface as the Windows DLL | `mock-driver/src/mockodbc.def` exports a deliberate 58-symbol set ("W variants ONLY for all string functions", mirroring MySQL's Unicode driver). `mock-driver/CMakeLists.txt` sets no `CXX_VISIBILITY_PRESET` and no version script, so the `.so` **also** exports 38 ANSI entry points Windows hides — `SQLGetDiagRec`, `SQLGetDiagField`, `SQLGetInfo`, `SQLTables`, `SQLColumns`, `SQLExecDirect`, `SQLPrepare`, `SQLDescribeCol`, `SQLConnect`, `SQLDriverConnect`, `SQLNativeSql`, `SQLGetTypeInfo` and 26 more, plus ODBC 2.x aliases (`SQLAllocEnv`, `SQLAllocConnect`, `SQLAllocStmt`, `SQLFreeEnv`, `SQLFreeConnect`). The W wrappers *delegate to* those ANSI functions (`unicode_wrappers.cpp:708-726`), so unixODBC can reach a less-exercised path the Windows DM cannot, and Windows and Linux are structurally different drivers built from one tree. Note this is **not** the explanation for the §8.1 diagnostic failures specifically — both `SQLGetDiagRec` and `SQLGetDiagRecW` resolve on Linux, so no fallback is triggered (see Part 0) — but it is a real HIGH defect on its own and it makes every other Linux-only finding harder to reason about. **Fix**: `-fvisibility=hidden` plus an explicit export list (or a version script generated from the `.def`), then re-measure **D1**. Pair with **F11** so it cannot regress. | |
| D3 | ❌ OPEN | HIGH | Registration parity | Make `register.sh` write the same driver attributes as `register.ps1` | `register.ps1` sets `APILevel=2`, `ConnectFunctions=YYY`, `DriverODBCVer=03.80`, `SQLLevel=1`, `FileUsage`, `UsageCount`. `register.sh:76-83` writes only `Description`, `Driver`, `Setup`, `FileUsage`, `UsageCount` — **no `APILevel`, no `ConnectFunctions`, no `DriverODBCVer`**. unixODBC consults these when deciding how to treat a driver, so the Linux and Windows CI slots are not testing the same registration. One-line fix; re-measure **D1** after it. | |
| D4 | ❌ OPEN | HIGH | Thread safety | `SQLFreeStmt` takes no `HandleLock` for any option | `statement_api.cpp:1644-1677`. The `fOption != SQL_DROP` guard wraps only `clear_diagnostics()`. `SQL_CLOSE` clears `result_data_`, `SQL_UNBIND` clears `column_bindings_`, `SQL_RESET_PARAMS` clears `parameter_bindings_` — all while a concurrent `SQLFetch` (`:759-905`) iterates them. This is the one violation of the README invariant inside the file (17 of the other 18 entry points comply) and it falsifies prior §3.2/§3.6. **Fix**: scope a lock over the non-`SQL_DROP` path so it releases before the handle dies. The invariant is also missed outside the file — `connection_api.cpp:14,50,126,275`, `transaction_api.cpp:12`, `info_api.cpp:693`, `driver_main.cpp` ×10. | |
| D5 | ❌ OPEN | HIGH | Thread safety | Catalog readers are unlocked, and `find_table` returns a pointer into a reallocatable vector | `mock_catalog.cpp:299-307` (`find_table`), `:373-385`, `:387-398`, `:400-420`, `:422-432`, plus the raw-reference accessors at `mock_catalog.hpp:125,137-138` — none take `mu_`. `execute_query` holds the returned `const MockTable*` across the whole executor (`mock_data.cpp:1413`) and dereferences it at `:1446,1531,1596,1671,1685`, while another connection's `CREATE TABLE` → `add_table` → `tables_.push_back` reallocates. That is a use-after-free, not just a race. Prior §3.1 fixed the five *snapshot* call sites but left these five. **Fix**: return `std::optional<MockTable>` by value, or snapshot once in `execute_query`. | |
| D6 | ❌ OPEN | HIGH | State isolation | Every `SQLDriverConnect` wipes the process-global catalog | `connection_api.cpp:102` calls `MockCatalog::instance().initialize(config.catalog)`, and `initialize` clears `tables_`, `indexes_`, `inserted_data_` **and** `procedures_` (`mock_catalog.cpp:25-30`). So connection B's connect destroys every row and table connection A created. `BehaviorController::set_config` (`:99`) likewise makes B's `Mode`/`FailOn`/`ResultSetSize` apply to A's in-flight statements, and `SQLEndTran(SQL_ROLLBACK)` on one connection clears the store for all (`transaction_api.cpp:47,71`). This is prior §3.8 (⏯️ DEFERRED as "shared rows") — it is destructive, not merely shared. Separate e2e *processes* are unaffected (each loads its own image); the exposure is multi-connection tests in one process, which is exactly the `cursor_stress` concurrent-statements shape. | |
| D7 | ❌ OPEN | HIGH | Spec conformance | `SQLGetData` has no chunked-retrieval state | `statement_api.cpp:910-1227`; `StatementHandle` has no per-column "bytes already returned" member. Calling `SQLGetData(hstmt, 1, SQL_C_CHAR, buf, 5, &len)` repeatedly returns the same first 4 characters forever and never returns `SQL_NO_DATA`. This is the most-tested `SQLGetData` requirement, and it is very likely the cause of the prior §8.4 "get-data same column twice" SKIP. **Fix**: `unordered_map<SQLUSMALLINT, size_t> getdata_offset_`, reset by `SQLFetch`/`SQLFetchScroll`/`SQL_CLOSE`. | |
| D8 | ❌ OPEN | HIGH | Memory safety | Three latent memory-corruption paths in handle and buffer management | (a) `handles.cpp:33-35` and `:50-53` range-`for` over `connections_`/`statements_` while each `delete` makes the child `erase` itself from that same vector — iterator invalidation UB; freeing an env/dbc with more than one child skips elements and reads freed storage. (b) `driver_main.cpp:164-169` deletes any valid `DescriptorHandle` unconditionally, so an app that frees an implicit descriptor obtained from `SQLGetStmtAttr` leaves `stmt->app_row_desc_` dangling and `~StatementHandle` deletes it again — spec requires `HY017`. (c) `statement_api.cpp:293-297,312-317` write `[0] = '\0'` into a caller buffer even when `buffer_length == 0`, and set `*str_len_or_ind` to the **truncated** length instead of the full one. | |
| D9 | ❌ OPEN | HIGH | Robustness | No exception barrier at the C ABI boundary, and `ResultSetSize` is unvalidated | `config.cpp:162` stores `ResultSetSize` verbatim as an `int` (negatives pass), reaching `mock_data.cpp:704` `data.reserve(row_count)` — `-1` becomes `SIZE_MAX` (`std::length_error`), `500000000` becomes `std::bad_alloc`. There is **no `try/catch` in any `SQL_API` entry point**, so the exception unwinds through `odbc32.dll` and terminates the host process. **Fix**: clamp at parse time and wrap every entry point in `catch (...) → HY001 / SQL_ERROR`. | |
| D10 | ❌ OPEN | HIGH | Spec conformance | Parameterised `WHERE` clauses are never substituted, and literal-`SELECT` parameter numbering is off | `substitute_params` (`statement_api.cpp:363-434`) has exactly three branches — `Insert`, `Call`, `is_literal_select`. A table `SELECT`/`UPDATE`/`DELETE` matches none, so the `?` survives into `where_clause` and is compared as the literal string `"?"`: `DELETE … WHERE id = ?` deletes nothing and reports 0. Separately, the literal-SELECT branch increments `param_idx` **before** testing `is_parameter_marker` (`:409`), so `SELECT 1, ?` looks up binding 2 for a marker bound as parameter 1 — the `Insert` branch (`:373-378`) increments inside the test and is correct. | |
| D11 | ❌ OPEN | HIGH | Spec conformance | Block-fetch attributes are accepted and ignored; the C-type switch in the fetch path is badly incomplete | `SQL_ATTR_ROW_ARRAY_SIZE` is stored (`statement_api.cpp:1593`) but `ROWS_FETCHED_PTR`, `ROW_STATUS_PTR`, `ROW_BIND_TYPE`, `ROW_BIND_OFFSET_PTR`, `ROW_OPERATION_PTR`, `USE_BOOKMARKS` all fall into `default: break;` (`:1636-1638`) and return `SQL_SUCCESS`; `SQLFetch` always advances one row and writes only element 0. Apps get success, one row, garbage in the rest, and an unwritten counter they read as uninitialised memory. Separately `SQLFetch`/`SQLBindCol` (`:808-891`) handle only 6 C types — `SQL_C_ULONG/USHORT/UBIGINT/STINYINT/UTINYINT/BIT/BINARY/NUMERIC/TYPE_DATE/TIME/TIMESTAMP` and **`SQL_C_WCHAR`** all fall to `default:` and are rendered as ANSI decimal strings; with the idiomatic `BufferLength = 0` for a fixed-size type the write is skipped entirely while the indicator claims a length. Prior §7.7 fixed the *parameter* switches; the fetch switches were not in scope and are still broken. Also: truncation on bound columns never returns `01004` (`:843-903`). `SQL_ATTR_PARAM_BIND_OFFSET_PTR` is stored and never dereferenced. | |
| D12 | ❌ OPEN | MED | Parser robustness | Malformed SQL crashes instead of returning `42000`, and an unparsed `WHERE` deletes the table | `mock_data.cpp`: `front()`/`back()` on possibly-empty strings at `:262` (`SELECT CAST( AS INTEGER)`), `:444` (`VALUES (1,,3)`), `:888,895,909` (`{fn YEAR()}`), `:1520` (`WHERE ID =`). `parse_literal` (`:491-492`) uses `std::stoll`/`stod` without checking the consumed length, so `WHERE a = 1 AND b = 2` parses as `a == 1` and silently drops the `AND`. Worst: `make_where_predicate` returns an always-**true** lambda for any predicate it does not understand (`:476,506,540,585`), and `DELETE` then erases the whole table (`:1708-1710`). **Fix**: guard the empty cases, use the `size_t* pos` overload, and return always-**false** plus a `42000` diagnostic on an unparsed predicate. | |
| D13 | ❌ OPEN | MED | Parser correctness | A second, weaker `WHERE` engine, plus five smaller parser gaps | `mock_data.cpp:1484-1552` re-implements SELECT filtering instead of calling `make_where_predicate`; it supports only `=` and `IN`, and `:1491`'s `find("=")` matches inside `<=`/`>=`/`!=`, so `WHERE ID >= 5` returns **all** rows unfiltered. Deleting the block in favour of the real predicate removes ~69 lines and fixes the operator set. Also: `ORDER BY` is only honoured when a `WHERE` is present (`:1557-1589`); ragged multi-tuple `INSERT` silently inserts one merged row while reporting the tuple count (`:1626-1659`); `preprocess_escape_sequences` rewrites `{…}` inside string literals (`:938`, unlike `find_close_brace` which tracks quotes correctly); `{?=CALL proc(…)}` does not parse at all (`:1020` passes it through, `parse_sql` only recognises a leading `CALL`/`EXECUTE PROCEDURE`) — the shape the README advertises as the port-3 canary; `UPDATE`/`DELETE` see only `inserted_data_`, so DML on a stock catalog table reports 0 rows while `SELECT` returns generated ones. | |
| D14 | ❌ OPEN | MED | Spec conformance | A cluster of smaller ODBC contract violations | `SQLNumParams` counts `?` inside string literals (`statement_api.cpp:1702-1704`) although `SQLDescribeParam` and `count_param_markers` both do quote-aware counting; a zero-row `SELECT` does not open a cursor (`:494`), so `SQLCloseCursor` then fails `24000`; `SQLRowCount` returns the result-set size for a `SELECT` instead of `-1` (`:501`) and never checks `executed_`; `SQL_CLOSE` leaves `executed_ = true` so a later `SQLFetch` returns `SQL_NO_DATA` instead of `24000`; `SQLNumResultCols`/`SQLDescribeCol` return nothing useful after `SQLPrepare` (`:522-559` never populates the metadata); `SQLDescribeCol` discards the parser's real `column_sizes` and hard-codes by type (`:1280-1292`); unknown attributes report success instead of `HY092`/`HYC00` (`:1554,1636,1656`); `cbSqlStr` is truncated from `SQLINTEGER` to `SQLSMALLINT` (`:471,546`), so a 40 KB statement becomes "Empty SQL statement"; `StatementHandle::row_count_` shadows `OdbcHandle::row_count_` (`handles.hpp:37` vs `:113`), so `SQL_DIAG_ROW_COUNT` always reports 0 and contradicts `SQLRowCount`. | |
| D15 | ❌ OPEN | MED | Buffer safety | `string_utils` narrows lengths to `SQLSMALLINT` and mishandles wide truncation on Windows | `string_utils.cpp:22` computes `src_len` as `SQLSMALLINT`, so a >32767-byte source goes negative and `memcpy` runs with a negative size plus a negative-index write. `:139-148`: on Windows, when the source exceeds `max_chars`, `MultiByteToWideChar` returns 0 with `ERROR_INSUFFICIENT_BUFFER` and writes nothing, so the caller gets an **empty** string plus `01004` instead of the truncated prefix — the POSIX branch truncates correctly, so this is a Windows/Linux behavioural divergence in the same area as **D1**. `:228` casts the wide byte total to `SQLSMALLINT`, so >16383 units reports a negative length that apps read as `SQL_NULL_DATA`. | |
| D16 | ✅ DONE | MED | Build | Apply warning flags to the mock driver | Done as part of **E1** — `mock-driver/CMakeLists.txt` now links `project_warnings`/`project_options` on both `mock-odbc-driver` and `mock-odbc-tests`. Production fixes: `info_api.cpp:299` `SQL_MAX_DRIVER_CONNECTIONS` now clamps `DriverConfig::max_connections` (an `int`) into the `SQLUSMALLINT` the info type requires instead of wrapping — `MaxConnections=70000` used to be reported as 4464; `mock_data.cpp:1233` cast a `size_t` through `long long` into `ParsedQuery::affected_rows` (an `int`), so the cast is now direct and honest. Test fixes: `test_dml.cpp:147,150` cast the computed parameter ordinal to `SQLUSMALLINT`; `test_silent_corruption.cpp:185` uses an `unsigned char` array for the UTF-8 `café` bytes, since `0xC3`/`0xA9` narrow under every spelling of a `char` braced list. Plus the six `toupper`/`tolower` lambda narrowings described in **E1**. | da4e33a |
| D17 | ❌ OPEN | HIGH | Test coverage | The mock's 107 tests avoid almost every surface where the defects above live | Grepping `mock-driver/tests/` for each API returns **zero** references to: `SQLBindCol`, `SQLDescribeCol`, `SQLNumResultCols`, `SQLSetStmtAttr`, `SQLGetStmtAttr`, `SQLMoreResults`, `SQLCancel`, `SQLNumParams`, `SQLDescribeParam`, `PARAMSET_SIZE`/`PARAM_STATUS_PTR`/`PARAMS_PROCESSED_PTR`/`ROW_ARRAY_SIZE`; **any `SQL…W` function at all** (so all 857 lines of `unicode_wrappers.cpp` and every string-conversion helper are untested, despite being the only entry points Windows exports); **any catalog function** (a single "column count matches the spec" test would have caught **D25**); `SQLGetFunctions`, `SQLEndTran`, `SQLColAttribute`, the cursor-name pair, the whole descriptor family, `SQLBulkOperations`/`SQLSetPos`/`SQLParamData`/`SQLPutData`, `SQLFetchScroll`. There is no `test_mock_data.cpp`, so the 1,724-line parser is exercised only indirectly and nothing feeds it malformed SQL; no `test_string_utils.cpp`; no truncation test anywhere (every diagnostic call uses an oversized buffer); no test allocates two connections. The tests that do exist assert correctly but sample the working fields — `GetDiagFieldTest` (`test_handles.cpp:293-330`) checks the three that work and never touches the four that are hard-wired (**D22**). **Highest value first**: add `test_connection.cpp` to the build (**D21**), then `test_catalog_shapes.cpp` (column counts for all ten), `test_unicode_wrappers.cpp` (length units, truncation, `01004`), `test_getdata.cpp` (chunking, WCHAR, `SQL_C_DEFAULT`, zero `BufferLength`), `test_mock_data.cpp` (malformed-SQL fuzz asserting `is_valid == false` and no crash), `test_bindcol.cpp`, `test_array_params.cpp`. | |
| D18 | ❌ OPEN | LOW | Maintainability | Split the two monolith files and share the conversion code | `execute_query` is **415** lines (`mock_data.cpp:1307-1721`), `SQLGetData` 319, `parse_sql` 267, `SQLExecute` 197, `evaluate_scalar_function` 175, `read_param_value` 159. Four independent C-type switches exist (`c_type_element_size`, `read_param_value`, `write_output_to_binding::write_int`, and the fetch/getdata delivery switches) — one shared descriptor table saves **~200 LoC** and closes **D11** in one place. The "copy into caller buffer with truncation" block is written **eight** times — one helper saves ~60 LoC and is the single place to fix **D7**, **D8c** and the missing `01004`. `SQLFetchScroll` (`driver_main.cpp:386-470`) is a verbatim copy of the `SQLFetch` bound-column loop (~85 LoC). Total: `statement_api.cpp` 1780 → ~1250, `mock_data.cpp` 1724 → ~1480. | |
| D19 | ❌ OPEN | LOW | Build | Link the mock's tests against an object library | `mock-driver/CMakeLists.txt:109-125` re-lists all 21 core sources for `mock-odbc-tests`, doubling compile time on every build. Use `add_library(mock_core OBJECT …)`. | |
| D20 | ❌ OPEN | LOW | Thread safety | `Mode=Random` shares an unlocked `mt19937` | `config.cpp:36-41`: a function-local `static std::mt19937` mutated concurrently by every entry point calling `config.should_fail(...)` on its own config copy, bypassing the locked `BehaviorController::should_fail`. | |
| D21 | ✅ DONE | HIGH | Test coverage | **`tests/test_connection.cpp` has never been in the build — 28 tests, 443 lines, never executed** | Added `tests/test_connection.cpp` to `mock-driver/CMakeLists.txt`. The suite count went **107 → 133**. The finding is worse than the plan recorded: the file did not merely never run, it **never compiled** — it was written against three APIs that do not exist (`BehaviorController::reset()`, `BehaviorController::configure_failure(fn, state, msg)`, `DriverConfig::connection_delay_ms`). Translated each to the real API: `set_config(DriverConfig{})`, `Mode=Partial` + `fail_on`/`error_code`, and `DriverConfig::latency`. Of the 28 tests, **25 passed immediately**. The 3 failures split two ways: (a) `SQLDisconnect_NotConnected` asserted `SQL_SUCCESS` with the comment "Disconnecting when not connected is OK" — **the test was wrong, not the mock**: the spec requires `SQL_ERROR` + `08003`, which is what the mock already returns, so the assertion was corrected and extended to check the SQLSTATE; (b) `SimulatedConnectionFailure` and `SimulatedConnectionTimeout` hit a real mock defect, now tracked as **D32** and scheduled in Phase 5 order 36 — they carry the `DISABLED_` prefix with their assertions intact, and dropping the prefix is D32's regression test. `DISABLED_` rather than `GTEST_SKIP()` because the skip macro returns and the unreachable body trips C4702 under the `/W4 /WX` that **E1** had just switched on. 133/133 green in both Debug and Release; 195/195 against the mock unchanged. | edd329a |
| D22 | ❌ OPEN | HIGH | Diagnostics | Four diagnostic header fields are never written, so `SQLGetDiagField` reports fiction | Grep confirms `return_code_`, `dynamic_function_`, `dynamic_function_code_` and `cursor_row_count_` appear only in `handles.hpp` and in the *reads* at `diagnostic_api.cpp:113,120,133,139`. So `SQL_DIAG_RETURNCODE` always reports `SQL_SUCCESS` even after `SQL_ERROR`, `SQL_DIAG_DYNAMIC_FUNCTION` is always empty, `SQL_DIAG_CURSOR_ROW_COUNT` always 0 — and `SQL_DIAG_ROW_COUNT` is hard-zero via the shadowing in **D14**. The record-0 vs record-N split itself is correct (`:101`, `:150`). Together these explain three of the eight probes in prior §8.1 without invoking any routing theory. Also: `SQLGetDiagRec`/`SQLGetDiagField` read `diagnostics_` with no `HandleLock` (`diagnostic_api.cpp:43,150`) — the README exempts them from *clearing*, not from *locking*. | |
| D23 | ❌ OPEN | HIGH | Memory safety | `SQLGetInfoW` copies the numeric result into the caller's buffer with no bounds check | `unicode_wrappers.cpp:434-437`: the string path correctly passes `cbInfoValueMax` to `copy_string_to_wbuffer`, but the numeric path is a bare `std::memcpy(rgbInfoValue, ansi_buf, ansi_len)` that never consults it. `SQLGetInfoW(dbc, SQL_GETDATA_EXTENSIONS, &two_byte_var, 2, &len)` writes 4 bytes. Since the `.def` exports only `SQLGetInfoW`, this is the path every Unicode driver manager takes on Windows. One-line fix. | |
| D24 | ❌ OPEN | HIGH | Spec conformance | Truncation is silent across the whole non-statement API — `01004` is never posted | `string_utils.cpp:37-40` returns `SQL_SUCCESS_WITH_INFO` but posts no diagnostic, and `sqlstate::STRING_TRUNCATED` is referenced **only** in `statement_api.cpp`. Six callers discard the return outright: `statement_api.cpp:1271` (`SQLDescribeCol`), `descriptor_api.cpp:194-200` (`SQLColAttribute`, hand-rolled memcpy with no signal at all), `connection_api.cpp:116-118` (`SQLDriverConnect` OutConnectionString) and `:200-205` (`SQL_ATTR_CURRENT_CATALOG`), `driver_main.cpp:306-317` (`SQLGetCursorName`), `info_api.cpp:723-725` (`SQLNativeSql`). Compounding it, `cbXMax * sizeof(SQLWCHAR)` is narrowed to `SQLSMALLINT` at `unicode_wrappers.cpp:148,266,354,740,837`, so a buffer >16383 characters wraps negative and `copy_string_to_wbuffer` returns `SQL_SUCCESS` **having written nothing**. This is the most likely cause of the prior §8.2 truncation and `SQLDescribeCol` probe failures. **Fix**: give the copy helpers an optional `OdbcHandle*` and post `01004` centrally; propagate the return at all six sites; widen the length arithmetic. | |
| D25 | ❌ OPEN | HIGH | Spec conformance | `SQLProcedureColumns` returns 8 of the 19 required columns | `catalog_api.cpp:577-581` emits `PROCEDURE_CAT, PROCEDURE_SCHEM, PROCEDURE_NAME, COLUMN_NAME, COLUMN_TYPE, DATA_TYPE, TYPE_NAME, COLUMN_SIZE` and omits `BUFFER_LENGTH, DECIMAL_DIGITS, NUM_PREC_RADIX, NULLABLE, REMARKS, COLUMN_DEF, SQL_DATA_TYPE, SQL_DATETIME_SUB, CHAR_OCTET_LENGTH, ORDINAL_POSITION, IS_NULLABLE`. Every **other** catalog shape is correct (Tables 5, Columns 18, PrimaryKeys 6, ForeignKeys 14, Statistics 13, SpecialColumns 8, Procedures 8, TablePrivileges 7, ColumnPrivileges 8), so this is an isolated omission — and a direct cause of the prior §8.3 catalog SKIPs. Related catalog gaps: `SQL_ATTR_METADATA_ID` is unimplemented (0 hits repo-wide); `matches_pattern` (`mock_catalog.cpp:469-509`) ignores the `\` escape the driver advertises via `SQL_SEARCH_PATTERN_ESCAPE` and treats an empty pattern as match-all; and the W catalog wrappers convert an empty-string argument to `NULL` (`unicode_wrappers.cpp:490-662`, 10 sites), which erases the ODBC distinction that `SQLTables`' enumeration modes depend on. | |
| D26 | ❌ OPEN | MED | Config | `FailOn` is inert unless `Mode=Partial`, and the mock's README does not say so | `config.cpp:29-31`: `should_fail` returns `false` unconditionally for `BehaviorMode::Success`, the default. So `Driver={Mock ODBC Driver};FailOn=SQLTables` silently does nothing. `README.md:205` documents the dependency; `mock-driver/README.md:26` lists only `Success, Failure, Random` and omits `Partial` entirely. **Fix**: either honour `FailOn` in `Success` mode (it is a per-function override, not a mode) or document it in both READMEs. | |
| D27 | ❌ OPEN | MED | Config | Connection-string parser edge cases the 11 adversarial tests miss | `config.cpp:66-94`: `in_braces` is a `bool` toggled by a `{` **anywhere**, not only at value start, so `Key=a{b;Mode=Failure` swallows the `;` and `Mode` is never parsed; an unterminated `{` absorbs the whole tail; the ODBC `}}` escape is unhandled so `PWD={a}}b}` closes at the first `}`. Separately `config.cpp:189-191` reads any non-`ms` `Latency` suffix as milliseconds — `Latency=10s` means 10 ms and `Latency=500us` means 0 ms. | |
| D28 | ❌ OPEN | MED | Transactions | No transaction state exists, and `SQL_ROLLBACK` discards committed rows | `ConnectionHandle` (`handles.hpp:66-95`) has no `in_transaction_` member, so `SQLDisconnect` (`connection_api.cpp:126-152`) can never return `25000`, and `SQLSetConnectAttr(SQL_ATTR_AUTOCOMMIT)` (`:244-246`) merely stores the value instead of committing on OFF→ON as the spec requires. `SQLEndTran` (`transaction_api.cpp:39,46,64,70`) treats any `fType != SQL_ROLLBACK` as commit (should be `HY012`) and calls `clear_inserted_data()` on rollback **even in autocommit-ON mode** — actively misleading for a mock whose purpose is validating "rows persisted" assertions. Its `should_fail` check at `:17-26` also runs before handle validation, so a garbage handle returns `SQL_ERROR` rather than `SQL_INVALID_HANDLE`. | |
| D29 | ❌ OPEN | MED | Spec conformance | Descriptor and attribute entry points return success for things they do not implement | `SQLColAttribute` returns `SQL_SUCCESS` with zeroed output for every unhandled field (`descriptor_api.cpp:274-277`) — including `SQL_DESC_COUNT`, `CONCISE_TYPE`, `OCTET_LENGTH`, `TYPE_NAME`, `LABEL`, `TABLE_NAME`, `BASE_COLUMN_NAME`, `UNNAMED`, `CASE_SENSITIVE`, `SEARCHABLE`; spec wants `HY091`. Same in `SQLGetDescField` (`:39-42`) and `SQLSetDescField` (`:68-70`). `SQLGetDescRec` explicitly discards the name output (`:100-102`). `SQLSetCursorName` discards the name and `SQLGetCursorName` synthesises one from the handle address (`driver_main.cpp:287-304`), so set-then-get does not round-trip and a heap pointer leaks into the name. `MaxConnections` is off by one — the constructor already pushed `this`, so `MaxConnections=1` rejects the very first connect (`connection_api.cpp:89-96`). `SQLGetFunctions` claims `SQLSetPos` and `SQLBulkOperations`, both of which always return `HYC00` (`info_api.cpp:596,644` vs `driver_main.cpp:481-502`). `SQLGetInfo`'s **types** are all correct (all ~90 cases audited against the spec table), but `SQL_*_CURSOR_ATTRIBUTES2`, `SQL_QUOTED_IDENTIFIER_CASE`, `SQL_CORRELATION_NAME`, `SQL_KEYWORDS`, `SQL_MAX_STATEMENT_LEN` and others fall through to `HY096`. | |
| D32 | ❌ OPEN | MED | Fault injection | `SQLConnect` is a fault-injection blind spot and resets the global config | Discovered by **D21**. `connection_api.cpp:14-47`: `SQLConnect` constructs a fresh `DriverConfig` ("use defaults for simple connect") and pushes it into `BehaviorController::instance().set_config()`, then connects unconditionally. It never calls `should_fail("SQLConnect")`, never calls `apply_latency()`, and never checks `max_connections` — all three of which its sibling `SQLDriverConnect` (`:76-96`) does. Two consequences: (1) `FailOn=SQLConnect` and `Latency=` are silently inert on this entry point, so the DSN-style connect path cannot be fault-injected at all; (2) because it *overwrites* the controller, a `SQLConnect` on any handle discards a configuration a previous `SQLDriverConnect` installed — the same process-global clobbering as **D6**, on a different code path. Two tests in `test_connection.cpp` are `DISABLED_` pending this fix: `DISABLED_SimulatedConnectionFailure` and `DISABLED_SimulatedConnectionTimeout`. **Fix**: read the current config rather than default-constructing one, then mirror `SQLDriverConnect`'s should_fail / apply_latency / max_connections preamble; drop the `DISABLED_` prefixes as the regression test. | |
| D30 | ❌ OPEN | MED | Install scripts | Harden the registration scripts | `register.sh:87` is not idempotent across a moved binary — `odbcinst -i -d` on an existing section bumps `UsageCount` instead of rewriting `Driver=`, so a stale path survives a rebuild into a new directory. `register.ps1:38-47` falls back to `HKCU\SOFTWARE\ODBC\ODBCINST.INI` when not elevated and still prints "registered successfully!", but the Windows DM reads `ODBCINST.INI` machine-wide only, so that registration does nothing. `register.ps1:46` has no WOW64 handling — run from 32-bit PowerShell it writes to `WOW6432Node` and 64-bit apps never see it — and its dev-layout probe at `:21` looks only in `..\build\Release\` while the checked-in tree builds to `build\Debug\`. See also **D3** (missing attributes) and **H6** (the stale `.reg`). | |
| D31 | ❌ OPEN | LOW | Maintainability | Collapse the boilerplate in the three large API files | `unicode_wrappers.cpp` (857 lines) is ~85% mechanical: ten catalog wrappers byte-identical modulo arity (`:477-663`, 187 lines → ~45), ten pure forwarders (~95 → ~30), and nine "wide out" wrappers that each repeat *ANSI temp → std::string → `copy_string_to_wbuffer(len × 2)` → `/2`* (~20 lines each). One `w_out()` helper reduces each to ~6 lines **and fixes D23 and D24 in a single place**. `info_api.cpp`'s 320-line `SQLGetInfo` switch (`:167-489`) is a textbook data table (~180 lines total), and the same table would replace the hand-maintained `is_string` duplicate at `unicode_wrappers.cpp:380-414`. `catalog_api.cpp` already has `setup_catalog_result`; what remains is a ten-times-repeated validate/lock/clear/`should_fail` preamble and ~35 copies of `row.push_back(x.empty() ? monostate{} : x)`. **Total ≈ −550 LoC.** | |

---

## Part E — Build system and static analysis

| ID | Status | Sev | Area | Task | Details / Evidence | Commit |
|---|---|---|---|---|---|---|
| E1 | ✅ DONE | HIGH | Warnings | Apply `project_warnings` to every target — it currently covers 3 of 8 | Linked `project_warnings` + `project_options` from the five targets that lacked them: `odbc_crusher_reporting`, the `odbc-crusher` executable, `odbc_crusher_tests`, `odbc_crusher_e2e_tests` (root project) and both mock targets. The mock is a separate CMake project, so rather than duplicating the flag list it now `include()`s the parent's `cmake/CompilerWarnings.cmake` via a relative module path, guarded by `if(NOT TARGET project_warnings)` and an `EXISTS` check that degrades to empty interface libraries with a `message(WARNING)` if `mock-driver/` is ever copied out of the repo. **Warning count corrected**: the plan predicted 3 app + 6 mock; the actual count was 3 app + 11 mock across 7 distinct sites — the six `[](unsigned char c){ return std::toupper(c); }` lambdas (`config.cpp:15`, `mock_catalog.cpp:13,33`, `mock_data.cpp:88,766`, `info_api.cpp:21`) each return `int` into a `char` and were invisible before because MSVC reports them at `<algorithm>:3800`, not at the call site. All fixed, `/WX` kept. Verified: both projects rebuild with zero warnings; 107/107 mock, 27/27 unit, 14/14 e2e, 195/195 against the mock. **Regression test**: `/WX` on MSVC *is* the test — any reintroduced narrowing fails the build. Note that the GCC/Clang branch of `CompilerWarnings.cmake` carries no `-Werror` (pre-existing design), so on Linux/macOS these flags warn but do not gate; see the new task **E5**. | da4e33a |
| E2 | ✅ DONE | MED | Build hygiene | Stop generating `version.hpp` into the source tree | `cmake/Version.cmake` now `configure_file()`s into `${CMAKE_BINARY_DIR}/generated/odbc_crusher/version.hpp` and exposes it through a new `odbc_crusher_version` INTERFACE target, linked by the two targets that actually include the header (`odbc-crusher` and `odbc_crusher_reporting`) — target-based rather than a global `include_directories()`, per AGENTS.md. Deleted the stale `include/odbc_crusher/version.hpp` from the working tree and dropped the `.gitignore` entry that hid it, leaving a comment there so nobody re-adds the entry together with a source-tree `configure_file()`. Verified with a from-scratch configure: the header lands in `build/generated/`, `--version` still reports `0.5.0` from the git tag, and `git status --untracked-files=all` shows nothing in `include/`. **Test**: a new `ci.yml` step, "Check the build did not write into the source tree", runs `git status --porcelain --untracked-files=all` after configure + build and fails on any output — so any *future* generated artifact that lands in the source tree fails CI instead of being papered over with another `.gitignore` line. This is a stronger check than the task asked for, and it is why the `.gitignore` entry had to go rather than merely being left harmless. | 1d668eb |
| E3 | ✅ DONE | MED | Build | Deduplicate the GoogleTest dependency | Picked "hoist the pin into `cmake/Dependencies.cmake`" over "make the mock a subdirectory of the main build". Reasoning: the mock is a standalone CMake project deliberately — `cmake -S mock-driver` is what `ci.yml`, `release.yml`, the register scripts and the docs all use, and it is built and shipped as its own artifact on all three platforms before the app is built — so folding it into the root build would couple two things that are separately released. The duplicate `_deps` tree is a cost; the version *drift* was the correctness problem, and that is what is fixed. `cmake/Dependencies.cmake` now holds all three pins (googletest v1.17.0, CLI11 v2.6.2, nlohmann/json v3.12.0) plus an `odbc_crusher_fetch_googletest()` macro used by both projects. **Diverged from the task text in one way**: rather than giving the mock a fallback pin for the case where `../cmake` is missing, the mock now hard-`FATAL_ERROR`s. A fallback would have meant a second literal `GIT_TAG v1.17.0` in the tree with a "keep in sync" comment — precisely the drift being removed. Nothing builds `mock-driver/` out of tree (checked `ci.yml` and `release.yml`), so the requirement costs nothing. **Test**: `grep -c googletest.git` over `cmake/Dependencies.cmake`, `CMakeLists.txt` and `mock-driver/CMakeLists.txt` returns 1/0/0 — there is exactly one pin, so drift is structurally impossible rather than merely discouraged; and the mock's `FATAL_ERROR` means a copy that tried to reintroduce one would not configure. Also updated both `actions/cache` keys in `ci.yml` to hash `cmake/Dependencies.cmake`, which is now where a bump happens — without that, a version bump would have silently reused a stale `_deps` cache. | 1d668eb |
| E5 | ❌ OPEN | LOW | Warnings | The warning interface has no `-Werror` on GCC/Clang, so it only gates MSVC | `cmake/CompilerWarnings.cmake` adds `/WX` in the MSVC branch but nothing equivalent in the GCC/Clang branch, so after **E1** Linux and macOS compile all 8 targets with `-Wall -Wextra -Wconversion -Wsign-conversion -Wold-style-cast` and ignore every result. The `AGENTS.md` checklist item "compiles without warnings on Windows **and Linux**" is therefore still only enforced on Windows. Two sub-questions, in order: (1) how many warnings does the mock actually emit on Linux now that the flags reach it — the mock is full of C-style casts like `(SQLCHAR*)"SELECT ..."` that `-Wold-style-cast` will flag by the hundred; (2) once that number is known, either fix them and add `-Werror`, or scope the flag set down on non-MSVC so the promise matches reality. Discovered while doing **E1**. | |
| E4 | ❌ OPEN | LOW | Tooling | Make the documented `clang-tidy` invocation actually work | `CMAKE_EXPORT_COMPILE_COMMANDS=ON` has **no effect** under the Visual Studio generator, so `clang-tidy -p build src/...` as documented in `CONTRIBUTING.md:585-587` cannot work as written — it needs a Ninja configure. Either document that or run clang-tidy in the Linux CI job, where it works. | |

---

## Part F — CI/CD, release, and stress-test

| ID | Status | Sev | Area | Task | Details / Evidence | Commit |
|---|---|---|---|---|---|---|
| F1 | ❌ OPEN | MED | Provenance | Verify the apt pins that have never run (carried from §7.1, restated) | `.github/drivers.json` pins `odbc-postgresql=1:16.00.0000-1` and `odbc-mariadb=3.1.15-3`; both differ from the strings prior §7.1 recorded. The manifest last changed 2026-04-27; the last **successful** postgresql / mariadb / mysql / clickhouse stress-test jobs ran 2026-02-09. Every run since has been firebird-only. Dispatch `gh workflow run stress-test.yml -f driver=all` once and record the outcome here. | |
| F2 | ❌ OPEN | MED | Robustness | Make a timed-out run still produce a JSON report | `.github/actions/run-crusher/action.yml:109,118` wrap crusher in `timeout --kill-after=30 570`, and `JsonReporter` serialises everything in `report_end` (`json_reporter.cpp:76-90`) — so a killed run yields **no JSON at all**, precisely when the report matters most. The text run survives via `tee`; the JSON run does not. **Fix**: a per-category watchdog inside crusher, or stream categories to the file as they complete. Same failure mode as **A9**; **G2** reduces the need for it. | |
| F3 | ❌ OPEN | MED | Release | Run the test suites in `release.yml` | The workflow runs a `--version` smoke test and nothing else — no `ctest`, and the `build-mock-driver` job runs no tests at all. A tag pushed to a commit that never passed `ci.yml` ships untested. **Fix**: `needs:` the CI workflow, or run `ctest -L unit` before packaging. | |
| F4 | ❌ OPEN | LOW | CI hygiene | Remove leftover debug output from the composite action | `.github/actions/run-crusher/action.yml:50-53` still prints `--- gh run list raw output: ---` plus the full run list on every stress-test job, from diagnostic commit `ddcf3cd`. The parsing bug it investigated was fixed in `e35a8a7`. | |
| F5 | ❌ OPEN | LOW | Duplication | Resolve `fetch-stress-test.ps1` (carried from §7.3) | 206 lines at the repo root with its own `$Drivers` hashtable duplicating `.github/drivers.json` — and **no firebird entry**, so the drift is real. It also writes to a `recommendations/` directory that no longer exists. Pick one: refactor it to read the manifest, or delete it and its output contract. | |
| F6 | ❌ OPEN | LOW | Security | Add `permissions:` blocks to `ci.yml` and `stress-test.yml` | `codeql.yml` and `release.yml` declare least-privilege scopes; the other two inherit the repository default. `stress-test.yml` hands `github.token` to `gh run download`. | |
| F7 | ❌ OPEN | LOW | Supply chain | Stop executing an unpinned remote script in CI | `.github/workflows/ci.yml:28`: `bash <(curl https://raw.githubusercontent.com/rhysd/actionlint/main/scripts/download-actionlint.bash)` runs whatever is on `main` today, on every push, before any other job. Pin to a tag/SHA or use the published action. | |
| F8 | ❌ OPEN | LOW | Manifest | Fix the dead `mock-driver.install.url` | `.github/drivers.json` declares `method: release_asset` pointing at `releases/latest/download/mock-odbc-driver-windows-x64.zip`, but `stress-test.yml:514` downloads `mock-odbc-driver-Windows-Release` from the latest master CI run. Misleading for anyone reading the manifest as the source of truth. Related: `release.yml` ships the mock for Windows only, though CI now builds it on all three platforms. | |
| F9 | ❌ OPEN | LOW | Coverage | Run the e2e suite in the Debug matrix slots too | `ci.yml` gates every mock-driver download and e2e step on `matrix.build_type == 'Release'`, so the Debug halves of the 6-slot matrix build the binary and never exercise it. Debug catches different bugs (assertions, iterator debugging) — and several **D**-series findings are exactly that class. | |
| F10 | ❌ OPEN | LOW | Portability | `timeout(1)` is absent on macOS runners | The composite action has a `macOS)` branch for artifact selection but calls GNU `timeout` unconditionally (`action.yml:109,118`). Latent today (no macOS stress-test job), but the action advertises support it does not have. | |
| F11 | ✅ DONE | MED | CI correctness | Make the `.def` drift check bidirectional | `ci.yml`'s "Check mockodbc.def export drift" step is now bidirectional and rests on a new sanctioned-exceptions file, `mock-driver/src/mockodbc.not-exported.txt`, holding the 38 symbols that are implemented but deliberately absent from the `.def` (the 7 ODBC 2.x aliases and the 31 ANSI entry points the W exports delegate to), each family with its rationale. Four assertions, all four proven to trip locally against doctored inputs: (1) `.def → source`, an export with no implementation — trips on a bogus `SQLNeverImplemented`; (2) **`source → .def`**, the direction that was missing and the reason **D2** went unnoticed — trips with `UNLISTED: SQLGetDiagRec` when that name is removed from the allow-list; (3) a name both exported and listed internal — trips on `SQLFetch`; (4) a listed name no longer implemented, so the allow-list cannot go stale — trips on `SQLLongGone`. Also replaced the extraction regex: the old `'^\s{4}SQL[A-Za-z0-9]+\s*$'` silently matched **zero** symbols against a `.def` reindented to 8 spaces, where the new comment-stripping/trimming parser still finds all 58 — verified. A `$exports.Count -lt 50` guard fails loudly if the extraction itself ever breaks, rather than passing vacuously. Current state: 58 exported + 38 deliberately internal = 96 implemented, consistent. | 3b4afbd |

---

## Part G — CLI and output contract

| ID | Status | Sev | Area | Task | Details / Evidence | Commit |
|---|---|---|---|---|---|---|
| G1 | ✅ DONE | HIGH | Output | `-o json` to stdout emits non-JSON — the documented pipe usage does not work | Chose **stderr** over "suppress in json mode" (the task offered both): stdout is the data channel and stderr the progress channel is the convention every consumer already expects, it keeps the progress line visible in console mode, and it gives **G2**/**G3** a rule to follow rather than a per-format special case. Two sites moved: `main.cpp`'s `"Phase 2: Running ODBC tests..."` and `json_reporter.cpp`'s `"JSON report written to: …"` — the latter so that `-o json -f report.json` leaves stdout completely empty. **Verified**: `odbc-crusher "…" -o json \| python -c "json.load(sys.stdin)"` now parses; it previously died on `Expecting value: line 1 column 1`. **Test**: new e2e scenario `JsonToStdoutIsParseableWithNoProgressChatter`, backed by a new harness entry point `run_crusher_stdout()` that runs `-o json` with **no** `-f` and captures stdout to a file. It asserts the first non-whitespace byte of stdout is `{` (so a failure names the offending chatter rather than just reporting a parse error), that the document parses, and that the progress line is still present on stderr — so the fix cannot regress into deleting the output instead of rerouting it. README's JSON Output section now documents the stream split. | be455bd |
| G2 | ❌ OPEN | MED | Usability | Add a category/test filter and `--list-tests` | There is no way to run a subset of the 195 probes. The e2e harness works around it in a comment: *"just ConnectionTests category isn't filterable from CLI, so we live with a full run"* (`tests/e2e/e2e_harness.cpp:122-123`). A filter speeds the harness up, makes bisecting a driver hang tractable, and reduces the urgency of **F2**. | |
| G3 | ❌ OPEN | MED | CI adoption | Add `--fail-on=<severity>` | `main.cpp:234` exits 1 if any probe is FAIL or ERR regardless of severity, so against a real driver the exit code is always 1 — which is why the stress-test composite must set `continue-on-error: true`. A severity threshold would let a driver project gate its own CI on "no CRITICAL regressions" instead of ignoring the exit code. | |
| G4 | ✅ DONE | LOW | Output contract | Version the JSON schema and use ISO-8601 timestamps | Added `schema_version` and converted `timestamp` to ISO-8601 UTC. Chose an **integer** version (`1`) rather than a `major.minor` string, matching the convention `.github/drivers.json` already uses in this repo; the semantics are documented on `JsonReporter::kSchemaVersion` and in README: bump when an existing key changes meaning, is renamed or is removed, keep it for purely additive changes, and consumers should reject a version they do not know. `timestamp` was a raw `std::time(nullptr)` integer that every consumer had to know was epoch seconds; it is now `YYYY-MM-DDThh:mm:ssZ` via a `gmtime_s`/`gmtime_r` + `strftime` helper. Grepped for consumers first — nothing in `tests/`, `.claude/skills/`, `.github/` or `fetch-stress-test.ps1` read `timestamp`, so the change breaks nothing. **Test**: new e2e scenario `ReportCarriesSchemaVersionAndIso8601Timestamp` asserts `schema_version` is the integer 1 and walks the timestamp character by character against the exact 20-character layout — hand-rolled rather than `<regex>` so that a 19- or 21-character variant cannot slip past. | be455bd |
| G6 | ❌ OPEN | LOW | Output contract | Keep raw timings out of the `actual` field so two reports diff cleanly | Found while checking the Phase 0 exit criterion "two consecutive runs produce byte-comparable reports". They very nearly do: of 2,023 leaf values, 171 differ between back-to-back runs and 169 of those are `duration_us` or the top-level `timestamp` — both expected. The other **two** are timings embedded in prose inside `actual`: `test_rapid_cursor_lifecycle` writes `"100/100 cycles completed in 5109 us (51 us/iteration)"` and `test_open_close_hammer_loop` writes `"500/500 cycles \| open_mean=32us close_mean=7us ratio=0.21875"`. That makes a before/after report diff show spurious changes in the one category most likely to need one. **Fix**: keep `actual` to the verdict-relevant facts (cycle counts, pass/fail ratio against the threshold) and move raw measurements to `duration_us` or a dedicated numeric field. Cheapest to do inside **A12**, which rewrites both of those strings anyway. | |
| G5 | ❌ OPEN | LOW | Reporting | Fix the console reporter's warnings and duplicate storage | `console_reporter.cpp:46,47` narrow `size_t` to `int`; `:9` never uses its `connection_string` parameter, so the banner ignores what it was given. `all_results_` keeps a full second copy of every `TestResult` purely for the severity-ranked summary. Surfaced by **E1**. | |

---

## Part H — Documentation and repo hygiene

| ID | Status | Sev | Area | Task | Details / Evidence | Commit |
|---|---|---|---|---|---|---|
| H1 | ❌ OPEN | MED | Docs | Correct the mock pass-rate claim in `README.md` | `README.md:254-255` states "**191/195 tests passing** (97.9%) … The four remaining failures are tracked mock-driver shortcomings". Measured today on Windows: **195/195, 100%**. Prior §7.7–§7.9 fixed those four and the README was never updated. (The per-category table at `:34-58` **is** accurate — all 23 rows match the live report.) | |
| H2 | ❌ OPEN | MED | Docs | Remove the "Firebird is not in the matrix" note | `README.md:322-324` says Firebird is "not yet in the matrix — intentionally deferred … the manifest schema reserves a slot", but `.github/drivers.json` has a full entry, `stress-test.yml` has a working job, and it is the only driver whose stress-test has run recently. Add it to the driver table at `:314-321`. | |
| H3 | ❌ OPEN | MED | Docs | Refresh `PROJECT_PLAN.md` | Says "Version 3.1", "Last Updated February 23, 2026", "12 test categories", "Test Categories (**131 tests**)" — actual is 23 categories and 195 probes. The dependency table names Google Test 1.14 / CLI11 2.4.2 / nlohmann 3.11.3; the build pins 1.17.0 / 2.6.2 / 3.12.0. Its mock schema section lists `CUSTOMERS`, contradicting `mock-driver/README.md` (`USERS`, `ORDERS`, `PRODUCTS`, `ORDER_ITEMS`) — and `catalog_depth_tests.cpp` believes the `PROJECT_PLAN` version (**A24**). | |
| H4 | ❌ OPEN | MED | Docs | Fix `CONTRIBUTING.md` and `AGENTS.md` drift | `CONTRIBUTING.md:32` still documents `src/cli/`, deleted in `543ec85`. `AGENTS.md` step 6 of "Adding New ODBC Tests" says "Add GTest unit tests in `tests/` and register in `tests/CMakeLists.txt`" — the per-category wrappers were retired in `cb2f639`; the current flow is an e2e scenario, as `CONTRIBUTING.md:545` correctly describes. Settle the include-order rule here too (**C14**). | |
| H5 | ❌ OPEN | MED | Legal | Add a `LICENSE` file | `README.md:387` says "MIT" and `mock-driver/README.md:111` says "MIT License - See LICENSE file". No `LICENSE` file exists anywhere in the repo. | |
| H6 | ❌ OPEN | LOW | Hygiene | Delete or regenerate `register_mock_driver.reg` | `mock-driver/install/register_mock_driver.reg:5-6` hard-codes `C:\temp\new\odbc-crusher\mock-driver\build\Release\mockodbc.dll` — a path from a previous checkout location. `register.ps1` supersedes it and is what CI uses. | |
| H7 | ❌ OPEN | LOW | Docs | Add `firebird` to the `/triage-driver` skill's driver list | `.claude/skills/triage-driver/SKILL.md:11-12` enumerates `postgresql, mariadb, mysql, duckdb, clickhouse, mock-driver`; firebird is missing although the manifest has it and the skill reads the manifest at runtime. | |
| H8 | ✅ DONE | MED | Process | Move this plan out of the gitignored `tmp/` directory | Created a tracked `docs/` directory and moved the three living planning documents into it: `IMPROVEMENT_PLAN.md` (this file, now `docs/IMPROVEMENT_PLAN.md`), `PORT_SQLCOMPONENTS_TESTS_PLAN.md` and `old-IMPROVEMENT_PLAN.md`. `PROJECT_PLAN.md:46` now links to the new path. `tmp/` stays gitignored for scratch artifacts, upstream clones and triage output; the verbatim duplicate `old-IMPROVEMENT_PLAN_INITIAL.md` was left there rather than committing two identical copies. **No test is possible** — this is a file move plus a doc link, and nothing executable references the path; the regression that matters (a living doc drifting back into `tmp/`) is a review concern, not a testable one. | 9015bb4 |
| H9 | ❌ OPEN | LOW | Hygiene | Decide where triage reports live, and track the new `CLAUDE.md` | `reports/DUCKDB-v1.5.2.0-ODBC-CRUSHER-REPORT.md` is committed at the repo root while the skill writes to `tmp/triage/<driver>/`. Separately, a 10-byte `CLAUDE.md` containing `@AGENTS.md` is untracked at the root — commit it or ignore it. | |
| H10 | ❌ OPEN | LOW | Docs | Document the DuckDB `SQL_DRIVER_VER` quirk in the triage report template | DuckDB returns `03.51.0000` (its ODBC compliance level) from `SQLGetInfo(SQL_DRIVER_VER)`. `SKILL.md:137-142` explains it, but `analyze.md`'s report template does not, so each generated report re-litigates it — the committed DuckDB report had to add the note by hand. (Prior §7.2.) | |

---

## Part I — Carried forward from the previous plan

Items from `old-IMPROVEMENT_PLAN.md` that remain genuinely open. Everything else in its §1–§7 is confirmed done (see Part 0 for the exceptions).

| ID | Status | Sev | Prior ID | Task | Details | Commit |
|---|---|---|---|---|---|---|
| I1 | ❌ OPEN | MED | §8.1 | Diagnostic forwarding through unixODBC (5 FAIL + 3 SKIP) | Now **D1**; test **D2**/**D3** before the `SQLError` hypothesis, which Part 0 argues against. | |
| I2 | ❌ OPEN | MED | §8.2 | SQLWCHAR width / Unicode path on Linux (1 FAIL + 3 SKIP) | Confirm crusher and the mock agree on `SQLWCHAR` width. Related: **A10** (the probe is wrong on 4-byte builds) and **D15** (Windows-only wide-truncation divergence). | |
| I3 | ❌ OPEN | LOW | §8.3 | Catalog search patterns + result-set shapes (6 SKIP) | Check first whether the same probes also SKIP on Windows at a severity the canary tolerates — **A24** and **B3** suggest a probe-side cause, not a mock gap. | |
| I4 | ❌ OPEN | LOW | §8.4 | Cursor-behaviour probes (3 SKIP) | "GetData same column twice" is almost certainly **D7** (no chunked-retrieval state in the mock). | |
| I5 | ❌ OPEN | MED | §8.5 | Array-parameter binding (8 SKIP) | Likely one shared precondition — see **D11** (block-fetch and array attributes accepted then ignored). Best ratio in the §8 set. | |
| I6 | ❌ OPEN | HIGH | §3.8 | Per-connection isolation of inserted rows | Re-opened as **D6** and upgraded from ⏯️ DEFERRED: the behaviour is destructive (each connect wipes the catalog), not merely shared. | |
| I7 | ⏯️ DEFERRED | LOW | PORT 3.E | `{?=CALL fn(?)}` return-value probe | Blocked on the mock exposing a callable *function*. Related to **A17** and **D13** (the mock's parser rejects `{?=CALL}` outright). | |
| I8 | ⏯️ DEFERRED | LOW | PORT 9.B / 10.B | Disconnect-with-open-transaction; pooled-connection recovery | Both blocked on the same missing capability — unblocked by **C13**. | |
| I9 | ❌ OPEN | LOW | §6.5 | Revisit the partial `param_binding_tests.cpp` migration | Now **C1**, with exact counts and the note that its stated blocker no longer exists. | |

---

## Part J — Recommended execution order

All 120 tasks, sequenced. The order is driven by **dependencies and risk**, not by severity: several high-severity items are deliberately late because fixing them earlier would be unverifiable or would be undone by a later refactor.

Three principles shape it:

1. **Build the safety net before changing behaviour.** Right now the mock's own tests avoid nearly every broken surface, the Linux canary tolerates 29 non-passing probes, and the JSON output cannot be parsed by a machine. Change probe verdicts before fixing that and you have no way to tell a fix from a regression.
2. **Land the shared helper before the twenty call sites that need it.** Four findings (**A1**, **A2**, **A4**, **B3**) are each a one-line change repeated dozens of times. Doing them by hand first means doing them twice.
3. **Fix the mock before chasing the Linux gap.** Several §8 clusters are probably *symptoms* of mock defects found in this review (**D22**, **D24**, **D25**), not of a platform issue. Chasing them first risks building platform-specific workarounds for bugs that are not platform-specific.

**Do not start with**: the big refactors (**C8**, **C9**, **D18**, **D31**) — they are the right destination but they move code you have not yet made correct; the §8 Linux clusters (**I1**–**I5**); or implementing `SQLError`, which Part 0 shows is not the blocker.

Each phase below is independently shippable and leaves the tree green. Phase 8 is documentation and can be done by anyone at any time, in parallel with everything else.

---

### Phase 0 — Turn on the instruments *(no behaviour change)*

| Order | Task | Note |
|---|---|---|
| 1 | **E1**, **D16** | Link the existing warning interface from all 8 targets; fix the 9 warnings it surfaces. `-Wshadow` alone catches **D14**/**D22**. |
| 2 | **D21** | Add `test_connection.cpp` to the mock build. Expect it to go red — that is the point. Whatever it reveals feeds Phase 5; record it here rather than fixing it now. |
| 3 | **G1**, **G4** | Progress text to stderr, `schema_version` + ISO-8601 in the report. Needed before any before/after report diffing. |
| 4 | **E2**, **E3** | `version.hpp` out of the source tree; one GoogleTest pin. Removes two sources of "why is my build stale" during the work ahead. |
| 5 | **F11** | Make the `.def` drift check bidirectional, so **D2** cannot regress once fixed. |

**Exit criteria**: warnings on everywhere and CI green; mock suite reports ~135 tests instead of 107; `odbc-crusher … -o json | jq .summary` works; two consecutive runs produce byte-comparable reports.

**Phase 0 result** — all met.

| Criterion | Measured |
|---|---|
| Warnings on everywhere | 8/8 targets link `project_warnings`; both projects rebuild clean under `/W4 /WX`. 14 warnings found and fixed, not the 9 predicted — see **E1**. |
| Mock suite grows | **107 → 133** (28 added, 2 of them `DISABLED_` pending **D32**). The plan's "~135" assumed all 28 would run. |
| `-o json \| jq` works | Verified end to end; previously failed with `Expecting value: line 1 column 1`. |
| Reports byte-comparable | Near enough to be useful: of 2,023 leaf values, 171 differ between back-to-back runs — 169 are `duration_us`/`timestamp`, and the remaining **2** are timings baked into `actual` by the cursor-stress probes. Recorded as **G6**. |
| Reference fixture | 195/195 on Windows throughout, unchanged by every commit in the phase. |
| Other suites | 27/27 unit, 14 → **16** e2e (two new scenarios for **G1**/**G4**). |

---

### Phase 1 — Stop losing results

| Order | Task | Note |
|---|---|---|
| 6 | **A9** | `run_test` catches `std::exception`. Until this lands, any later fix that introduces a throw silently destroys all 195 results. |
| 7 | **D9** | Exception barrier at the mock's C ABI boundary + clamp `ResultSetSize`. Stops a malformed connection string terminating the host process. |
| 8 | **G5** | Fix the console-reporter warnings **E1** surfaced. |
| 9 | **F2** | A timed-out run still produces JSON. Same failure mode as **A9**, on the CI side. |

**Exit criteria**: no single probe, and no connection-string value, can destroy a run or the report.

---

### Phase 2 — Localized probe fixes *(no refactor)*

Each of these is a self-contained bug with an obvious correct answer. Do them in one pass; they are cheap and they immediately stop the tool lying.

| Order | Task | Note |
|---|---|---|
| 10 | **A5**, **A6**, **A7** | The three outright logic bugs: `strlen` UB on an unterminated buffer, `\|\|` where `&&` was meant, a comparison that was never written. |
| 11 | **A3** | Clamp every driver-supplied length before using it as a buffer length. Handle `SQL_NO_TOTAL`. |
| 12 | **A8**, **A21** | Probes that FAIL correct drivers (**A8**) or PASS broken ones (**A21**). |
| 13 | **A11** | `== SQL_SUCCESS` → `SQL_SUCCEEDED` across all 13 sites; distinguish "hit the row cap" from "reached the end". |
| 14 | **A12**, **G6** | Cursor-stress thresholds: nanosecond totals, an absolute floor, a trimmed statistic, and sample the diagnostic queue at the right moment. **G6** (added during Phase 0) rewrites the same two `actual` strings to stop baking raw timings into them. |
| 15 | **A10**, **A13**, **A25** | Width and spec-accuracy fixes: surrogate pairs on 4-byte `SQLWCHAR`, truncate-not-round in the NUMERIC probe, `SQL_DIAG_NUMBER` into an `SQLINTEGER`. |
| 16 | **A16**, **A17**, **A18**, **A26** | Drain `SQLMoreResults` before reading output params; cap the catalog walks; accept `SQL_PARAM_TYPE_UNKNOWN`; the small-nits sweep (missing `<cmath>` first — it can silently disable every numeric comparison). |

**Exit criteria**: 195/195 still holds against the mock, and each fixed probe has a mock configuration that proves it can now fail.

---

### Phase 3 — Build the shared helpers, then use them

This is the structural heart of the work. Each helper is written once and then applied; do not apply by hand first.

| Order | Task | Note |
|---|---|---|
| 17 | **C2** | `TestBase::execute_first_working` — returns the winning query and records `(query, sqlstate, message)` for every failure. |
| 18 | **A2** | Route the 58 bare `SELECT`s through it. This is what makes the Escape Sequence, Numeric Struct and Cursor Stress categories work against Firebird. |
| 19 | **A1** | With the helper in place, a successful execute becomes conclusive: compare and FAIL instead of falling through to SKIP. Closes prior §2.8 properly. |
| 20 | **C5** → **B3** | Extract `first_sqlstate`, then `classify_failure`, then apply at the ~40 sites that currently choose SKIP without reading a SQLSTATE. |
| 21 | **A4** | Escape-sequence helpers return the SQLSTATE instead of `'NULL'`, and feed it to **B3**. |
| 22 | **C10** → **C4** | Make `RoundTripTableGuard` movable, then adopt it in the three files that reimplement it. |
| 23 | **A14**, **A15**, **A22**, **A19** | The transaction/cleanup fixes, done here because **C4** has just touched the same probes: autocommit save/restore, stale-table clearing, commit-rc capture, `verify_rows_persisted` hardening. |
| 24 | **C1** | Finish the `param_binding_tests.cpp` migration (closes **I9**). Its stated blocker no longer exists once **C4** lands. |
| 25 | **C3** | Delete the ~45 inner `try/catch (std::exception)` blocks made redundant by **A9**. |

**Exit criteria**: one fallback helper, one classification helper, one table guard, all used everywhere; `param_binding_tests.cpp` fully migrated; ~600 LoC removed.

---

### Phase 4 — Make the pass rate mean something

| Order | Task | Note |
|---|---|---|
| 26 | **B2** | Add the reported-but-unscored status. Everything else in this phase depends on it. |
| 27 | **B1** | Fix or reclassify the ≥26 always-PASS probes. Most have an obvious missing assertion; the rest become informational. |
| 28 | **A23** | Reclassify the eight probes that measure the Driver Manager, or redesign them to reach the driver. |
| 29 | **C7** → **A24**, **A20** | Extract `discover_tables` and `get_string`, then remove the hardcoded `CUSTOMERS`, add the ClickHouse `ENGINE` variant and the Firebird-safe DECIMAL precision, and finish the round-trip matrix. |
| 30 | **B4**, **B6**, **B7** | Stop reporting infrastructure errors as unsupported features; report partial success; reconcile the contradictory verdicts and the wrong conformance tag. |
| 31 | **B8**, **B9**, **B10** | Text, naming and the error-clearing comparison. |

**Exit criteria**: every PASS in the report is a probe that could have failed; every SKIP names the SQLSTATE that justified it. The advertised pass rate changes — update `README.md` (**H1**) in the same commit.

---

### Phase 5 — Mock driver correctness

The mock is the reference fixture. Until it is right, "195/195" means little, and the Linux gap cannot be diagnosed.

| Order | Task | Note |
|---|---|---|
| 32 | **D18** | The shared C-type descriptor table and the single "copy into caller buffer with truncation" helper. This one refactor is where **D7**, **D11**, **D23** and **D24** are actually fixed. |
| 33 | **D24**, **D23** | Post `01004` centrally; bound the `SQLGetInfoW` memcpy; widen the `SQLSMALLINT` length arithmetic. |
| 34 | **D7**, **D11** | Chunked `SQLGetData` (the `offset` parameter the new helper already takes); block-fetch attributes and the C-type gaps in the fetch path. |
| 35 | **D8**, **D15** | The three memory-safety paths (destructor iterator invalidation, implicit-descriptor double free, zero-length buffer write) and the `string_utils` narrowing plus the Windows-only wide-truncation divergence. |
| 36 | **D4**, **D5**, **D6**, **D20**, **D32** | Concurrency and shared state: the missing `SQLFreeStmt` lock, unlocked catalog readers, the connect-wipes-everything behaviour (closes **I6**), the shared RNG, and `SQLConnect`'s config clobbering + fault-injection blind spot (**D32**, added during Phase 0 by **D21**). |
| 37 | **D10**, **D12**, **D13** | Parameter substitution and the off-by-N; then parser robustness — the empty-string UB, partial numeric parses, and the unparsed-`WHERE`-deletes-everything fallback. |
| 38 | **D22**, **D14** | Populate the diagnostic header fields and remove the `row_count_` shadowing. Do this *before* Phase 6 — it likely accounts for three of the eight §8.1 probes on its own. |
| 39 | **D25**, **D28**, **D29** | `SQLProcedureColumns`' missing 11 columns and the catalog semantics; transaction state; the descriptor and attribute entry points that report success for unimplemented behaviour. |
| 40 | **D26**, **D27** | `FailOn` gating and the connection-string parser edge cases. |
| 41 | **D17** | Grow the mock's tests alongside each fix above, not after. Start with the catalog column-count test that would have caught **D25**. |
| 42 | **D19**, **D31** | Object library for the tests; collapse the `unicode_wrappers` / `info_api` / `catalog_api` boilerplate (≈ −550 LoC). |

**Exit criteria**: mock suite substantially larger and green; 195/195 still holds on Windows; every fix above has a test that would catch its regression.

---

### Phase 6 — Linux parity

**Run the two cheap experiments early** — during Phase 0 or 1, as a one-hour measurement — and record the numbers here. But do the *fixing* now, after Phase 5, because several clusters should have shrunk on their own.

| Order | Task | Note |
|---|---|---|
| 43 | **D2**, **D3** | Export surface (`-fvisibility=hidden` + explicit list) and registration parity. Re-measure after each, separately, so you know which one moved the number. |
| 44 | **D1** | Re-measure the Linux baseline. Compare against the pre-Phase-5 numbers. |
| 45 | **I1** | Whatever remains of the diagnostic cluster. If it is still non-zero, `ltrace` the `.so` to see which entry point unixODBC calls and when — that single observation discriminates between the registration and the clear-on-entry hypotheses. |
| 46 | **I5**, **I2** | The array-parameter cluster (8 SKIPs, likely one precondition — see **D11**) and the `SQLWCHAR` width audit. |
| 47 | **I3**, **I4** | Catalog patterns and cursor behaviour — check first whether **D25** and **D7** already closed them. |
| 48 | **B5** | Once the count reaches zero, delete the tolerance constants and restore the hard `failed == 0 && skipped == 0` contract prior §7.10 intended. |

**Exit criteria**: `kMaxFailed` and `kMaxSkipped` deleted from `test_e2e_scenarios.cpp`; Linux, Windows and macOS all report 195/195.

---

### Phase 7 — Remaining engineering

| Order | Task | Note |
|---|---|---|
| 49 | **G2**, **G3** | `--category` / `--filter` / `--list-tests`, then `--fail-on=<severity>`. Both make the tool adoptable by a driver project's own CI, and **G2** speeds up the e2e harness. |
| 50 | **C13** → **I8** | Give `TestBase` the connection string; implement the two ports blocked on it. **I7** stays deferred until the mock exposes a callable function. |
| 51 | **F1** | Dispatch a full stress-test run and verify the apt pins that have never executed. |
| 52 | **F3**, **F9** | Run the test suites in `release.yml`; exercise e2e in the Debug matrix slots. |
| 53 | **F4**, **F5**, **F6**, **F7**, **F8**, **F10** | CI hygiene, security and manifest cleanups: drop the leftover debug output, resolve `fetch-stress-test.ps1`, add `permissions:` blocks, stop the unpinned `curl \| bash`, fix the dead manifest URL, and the macOS `timeout` gap. |
| 54 | **D30**, **E4**, **E5** | Registration-script hardening; make the documented clang-tidy invocation actually work; decide whether the GCC/Clang warning branch gets `-Werror` (**E5**, added during Phase 0). |
| 55 | **C6**, **C11**, **C14** | Dead code, overlapping probes, and settle the include-order rule. |
| 56 | **C8**, **C9**, **C12** | The three big table-driven rewrites (≈ −950 LoC). Deliberately last: they are pure structure, and by now the code they move is correct and covered. |

**Exit criteria**: no known correctness debt outstanding; the remaining backlog is documentation.

---

### Phase 8 — Documentation *(parallel, any time)*

| Order | Task | Note |
|---|---|---|
| — | **H8** | Do this one **first**, before anything else in the plan: move the living plans out of gitignored `tmp/` so this document is visible to the people expected to follow it. |
| — | **H1**, **H2** | The pass-rate and Firebird-matrix corrections. **H1** must be redone after Phase 4 changes the number. |
| — | **H3**, **H4** | `PROJECT_PLAN.md` and the `CONTRIBUTING.md` / `AGENTS.md` drift. |
| — | **H5** | Add the `LICENSE` file. Two minutes, and the repo currently references a file that does not exist. |
| — | **H6**, **H7**, **H9**, **H10** | The stale `.reg`, the skill's driver list, report locations, and the DuckDB version quirk in the report template. |

---

### Sequencing at a glance

| Phase | Tasks | Character | Risk |
|---|---:|---|---|
| 0 | 8 | Build and tooling, no behaviour change | Very low |
| 1 | 4 | Robustness | Low |
| 2 | 15 | Localized probe fixes | Low |
| 3 | 14 | Shared helpers + their call sites | Medium — largest diff, best covered by Phase 0's net |
| 4 | 12 | Result taxonomy | Medium — changes the advertised pass rate |
| 5 | 24 | Mock driver correctness | Medium-high — the mock is the fixture everything else trusts |
| 6 | 9 | Linux parity | Low once Phase 5 lands |
| 7 | 21 | CLI, CI, structural cleanup | Low |
| 8 | 10 | Documentation | None — parallelisable |
| — | 3 | **I6**, **I7**, **I9** — pointer entries, not independent work | I6 is closed by **D6**, I9 by **C1**, I7 stays ⏯️ DEFERRED |
| | **120** | | |

Every task in Parts A–I appears exactly once above; the count was checked mechanically against the task tables.

Per the maintenance rules at the top: one task per commit where practical, and update this file's Status and Commit columns in the same commit.

---

## Appendix — Reproducing the measurements

```bash
# Full local build + test (Windows, MSVC)
cmake -B mock-driver/build -S mock-driver && cmake --build mock-driver/build --config Debug -j 8
ctest --test-dir mock-driver/build -C Debug --output-on-failure          # 107/107
cmake -B build && cmake --build build --config Debug -j 8
ctest --test-dir build -C Debug --output-on-failure -L unit              # 27/27 (7 skip)
ctest --test-dir build -C Debug --output-on-failure -L e2e               # 14/14

# The reference fixture — expect 195/195 on Windows/macOS
build/src/Debug/odbc-crusher.exe "Driver={Mock ODBC Driver};Mode=Success;Catalog=Default;ResultSetSize=10;" -o json -f report.json

# E1 / D16 — surface the warnings the current CMake configuration hides
cmake -B cmake-build-warn-app  -DCMAKE_CXX_FLAGS="/W4 /EHsc" && cmake --build cmake-build-warn-app  --config Debug -j 8
cmake -B cmake-build-warn-mock -S mock-driver -DCMAKE_CXX_FLAGS="/W4 /EHsc" && cmake --build cmake-build-warn-mock --config Debug -j 8

# G1 — the documented pipe usage
build/src/Debug/odbc-crusher.exe "Driver={Mock ODBC Driver};Mode=Success;" -o json | python -c "import sys,json; json.load(sys.stdin)"

# A2 — bare SELECTs with no Firebird fallback, per file
cd src/tests && for f in *_tests.cpp; do
  bare=$(grep -oE '"SELECT [^"]*"' "$f" | grep -vi ' FROM ' | wc -l)
  fb=$(grep -c 'RDB\$DATABASE' "$f")
  [ "$bare" -gt 0 ] && [ "$fb" -eq 0 ] && printf "%-32s %s bare selects, 0 fallbacks\n" "$f" "$bare"
done

# C1 — run_test adoption per file
for f in *_tests.cpp; do printf "%-32s run_test=%-3s make_result=%-3s clock=%s\n" "$f" \
  "$(grep -c 'run_test(' $f)" "$(grep -c 'make_result(' $f)" "$(grep -c 'high_resolution_clock::now()' $f)"; done

# D2 — compare the Windows export list against what the sources define
grep -oE '^\s+SQL[A-Za-z0-9]+' mock-driver/src/mockodbc.def | tr -d ' \r' | sort -u > /tmp/exported.txt
grep -rhoE 'SQLRETURN\s+SQL_API\s+[A-Za-z0-9_]+\s*\(' mock-driver/src --include=*.cpp \
  | grep -oE 'SQL[A-Za-z0-9_]+' | grep -v '^SQL_API$' | sort -u > /tmp/defined.txt
comm -23 /tmp/defined.txt /tmp/exported.txt   # 38 ANSI entry points exported on Linux only
```
