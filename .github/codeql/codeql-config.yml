# CodeQL config — docs/old-IMPROVEMENT_PLAN.md §6.3 (the `§N.M` numbering is
# the old plan's; the current docs/IMPROVEMENT_PLAN.md uses lettered row IDs)
#
# Without this config, CodeQL analyses the fetched FetchContent sources
# (googletest, nlohmann/json, CLI11) under build/_deps/. That produced
# ~14 of the 16 high-severity alerts on the first run — all in
# third-party code we don't own and don't want to maintain warnings for.
#
# `paths-ignore` doesn't reliably filter compiled-language analysis
# (CodeQL analyses anything that ends up in the compilation database,
# regardless). Use the inverse `paths:` (positive list) — explicitly
# scope analysis to our source trees only.
name: ODBC Crusher CodeQL config

paths:
  - 'src'
  - 'mock-driver/src'
  - 'tests'
  - 'include'
