cmake_minimum_required(VERSION 3.15)
project(MockDriverSimpleTest)

set(CMAKE_CXX_STANDARD 17)

# Find ODBC
find_package(ODBC REQUIRED)

# Create executable
add_executable(test_simple test_simple.cpp)
target_link_libraries(test_simple ${ODBC_LIBRARIES})
target_include_directories(test_simple PRIVATE ${ODBC_INCLUDE_DIRS})
